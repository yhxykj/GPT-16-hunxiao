
import Foundation

import UIKit
import Speech
import AVFAudio
import AdSupport
import Accelerate
import SVProgressHUD

enum KDChuang6 {
    case KDPrefixPlay
    case KDDelegate
}

class LQBuffer: NSObject {
var can_Sum: Bool = false
var audioSpace: Double = 0.0


    
    let utils: NeoNuiTts = NeoNuiTts.get_instance()
    let voicePlayer: MYGAnswerScreen = MYGAnswerScreen()
    typealias CompletionHandler = (KDChuang6) -> Void
    var completionHandler: CompletionHandler?
    
    static let shared: LQBuffer = {
       var avatarsA: [String: Any]! = [String(cString: [106,115,105,109,100,99,102,103,0], encoding: .utf8)!:String(cString: [115,101,118,101,114,105,116,121,0], encoding: .utf8)!, String(cString: [112,114,101,118,105,101,119,115,0], encoding: .utf8)!:String(cString: [98,97,116,99,104,105,110,103,0], encoding: .utf8)!, String(cString: [119,111,114,107,101,114,115,0], encoding: .utf8)!:String(cString: [112,114,107,0], encoding: .utf8)!]
    var configk: [String: Any]! = [String(cString: [100,120,116,111,114,121,0], encoding: .utf8)!:5653.0]
    var click8: Float = 2.0
   while ((2 & configk.values.count) > 1 && 3 > (2 & configk.values.count)) {
      configk["\(click8)"] = Int(click8)
      break
   }
   if configk.values.count >= avatarsA.keys.count {
      configk["\(click8)"] = 2
   }

        let instance = LQBuffer()
      click8 -= Float(Int(click8))
   while ((configk.keys.count ^ 1) == 5 && (configk.keys.count << (Swift.min(labs(1), 1))) == 5) {
       var networkg: String! = String(cString: [114,101,102,108,101,99,116,111,114,0], encoding: .utf8)!
       var stringy: String! = String(cString: [97,101,115,111,112,116,0], encoding: .utf8)!
       var eventI: String! = String(cString: [112,97,114,101,110,116,0], encoding: .utf8)!
       var verticalC: Double = 5.0
       var present8: [String: Any]! = [String(cString: [121,117,118,114,103,98,97,0], encoding: .utf8)!:true]
      if networkg.count <= eventI.count {
         eventI.append("\(networkg.count)")
      }
         present8 = ["\(present8.keys.count)": present8.values.count]
      while (5.5 == (verticalC + Double(eventI.count)) && 3.74 == (5.5 + verticalC)) {
         verticalC -= (Double(stringy == (String(cString:[51,0], encoding: .utf8)!) ? stringy.count : eventI.count))
         break
      }
          var recognizedz: String! = String(cString: [99,111,109,109,105,116,116,101,100,0], encoding: .utf8)!
         verticalC *= Double(3)
         recognizedz = "\(recognizedz.count)"
      if stringy.hasSuffix("\(verticalC)") {
         stringy.append("\(networkg.count)")
      }
         networkg = "\(Int(verticalC) ^ 3)"
      for _ in 0 ..< 3 {
          var removeW: Double = 5.0
          var promptM: String! = String(cString: [99,117,116,0], encoding: .utf8)!
          var infoN: String! = String(cString: [110,101,101,100,101,100,0], encoding: .utf8)!
          var modeltypeW: String! = String(cString: [111,114,100,101,114,0], encoding: .utf8)!
          var observations9: [String: Any]! = [String(cString: [109,111,100,117,108,101,115,0], encoding: .utf8)!:String(cString: [109,118,112,114,111,98,115,0], encoding: .utf8)!, String(cString: [116,106,117,116,105,108,0], encoding: .utf8)!:String(cString: [99,111,114,100,122,0], encoding: .utf8)!]
         verticalC -= Double(1 << (Swift.min(2, networkg.count)))
         removeW *= (Double(infoN == (String(cString:[122,0], encoding: .utf8)!) ? infoN.count : modeltypeW.count))
         promptM.append("\(1 + Int(removeW))")
         modeltypeW = "\((promptM == (String(cString:[102,0], encoding: .utf8)!) ? infoN.count : promptM.count))"
         observations9 = ["\(observations9.values.count)": observations9.values.count << (Swift.min(modeltypeW.count, 1))]
      }
      repeat {
         networkg.append("\(stringy.count)")
         if networkg == (String(cString:[118,104,107,99,0], encoding: .utf8)!) {
            break
         }
      } while (stringy == String(cString:[55,0], encoding: .utf8)!) && (networkg == (String(cString:[118,104,107,99,0], encoding: .utf8)!))
      if 4 == (3 | stringy.count) && 3 == (3 | present8.values.count) {
          var questionO: String! = String(cString: [116,101,115,116,101,114,0], encoding: .utf8)!
          var removeb: String! = String(cString: [109,97,105,110,102,117,110,99,0], encoding: .utf8)!
         stringy = "\(questionO.count)"
         questionO.append("\(((String(cString:[78,0], encoding: .utf8)!) == removeb ? removeb.count : removeb.count))")
      }
          var evetl: [String: Any]! = [String(cString: [114,101,103,105,115,116,114,121,0], encoding: .utf8)!:9848.0]
          var detailsF: [Any]! = [215, 131, 470]
         eventI.append("\(eventI.count >> (Swift.min(2, labs(Int(verticalC)))))")
         evetl = ["\(evetl.count)": 3]
         detailsF.append(detailsF.count | 3)
         stringy = "\(1)"
         eventI.append("\((networkg == (String(cString:[54,0], encoding: .utf8)!) ? networkg.count : stringy.count))")
      while (5 == (3 >> (Swift.min(5, present8.keys.count))) || (eventI.count >> (Swift.min(labs(3), 5))) == 5) {
          var recognizedZ: [String: Any]! = [String(cString: [99,108,105,112,102,0], encoding: .utf8)!:133, String(cString: [109,111,99,107,0], encoding: .utf8)!:664, String(cString: [116,97,112,116,105,99,0], encoding: .utf8)!:731]
          var headersr: Int = 4
         present8["\(headersr)"] = networkg.count - headersr
         recognizedZ["\(recognizedZ.count)"] = 3 & recognizedZ.values.count
         break
      }
         verticalC += (Double(stringy == (String(cString:[109,0], encoding: .utf8)!) ? Int(verticalC) : stringy.count))
      for _ in 0 ..< 3 {
          var evetH: Float = 5.0
          var namelabelY: Float = 1.0
         eventI.append("\(networkg.count)")
         evetH += Float(2 & Int(evetH))
         namelabelY += Float(1)
      }
      configk["\(configk.count)"] = 1
      break
   }
        instance.utils.delegate = instance
   repeat {
      click8 += Float(configk.keys.count)
      if click8 == 1110748.0 {
         break
      }
   } while (click8 == 1110748.0) && ((Float(avatarsA.count) * click8) >= 2.33 || (click8 * Float(avatarsA.count)) >= 2.33)
   for _ in 0 ..< 3 {
      configk["\(click8)"] = 1
   }
        instance.voicePlayer.delegate = instance
        return instance
    }()

@discardableResult
 func orderNameIndexSupportHeightData(successGundong: [Any]!, aymentNickname: Double, true_pcRmblabel: Float) -> Double {
    var generateu: Int = 1
    var changep: String! = String(cString: [118,109,115,108,0], encoding: .utf8)!
   repeat {
       var valueb: String! = String(cString: [105,97,116,0], encoding: .utf8)!
       var jiaoP: String! = String(cString: [108,111,103,105,99,0], encoding: .utf8)!
       var resumer: String! = String(cString: [98,111,117,110,100,115,0], encoding: .utf8)!
       var answerw: String! = String(cString: [116,105,110,116,101,114,108,97,99,101,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
         answerw = "\(((String(cString:[84,0], encoding: .utf8)!) == answerw ? resumer.count : answerw.count))"
      }
      repeat {
          var dataj: Double = 0.0
         resumer.append("\(answerw.count << (Swift.min(4, valueb.count)))")
         dataj -= Double(1 + Int(dataj))
         if resumer == (String(cString:[51,55,119,53,0], encoding: .utf8)!) {
            break
         }
      } while (valueb == resumer) && (resumer == (String(cString:[51,55,119,53,0], encoding: .utf8)!))
      for _ in 0 ..< 3 {
          var launch6: String! = String(cString: [117,110,105,116,115,0], encoding: .utf8)!
          var liholderlabelf: Double = 0.0
          var iose: Double = 5.0
         jiaoP.append("\(Int(iose))")
         launch6 = "\(Int(liholderlabelf) << (Swift.min(labs(3), 1)))"
         liholderlabelf *= Double(Int(liholderlabelf) & launch6.count)
      }
      if resumer.count >= 1 {
         resumer.append("\(answerw.count & resumer.count)")
      }
      for _ in 0 ..< 3 {
         valueb.append("\(((String(cString:[68,0], encoding: .utf8)!) == jiaoP ? jiaoP.count : valueb.count))")
      }
          var messagek: Bool = true
          var bar0: Float = 5.0
         answerw.append("\(jiaoP.count % 1)")
         messagek = !messagek && bar0 < 13.76
         bar0 *= Float(1 / (Swift.max(Int(bar0), 6)))
          var bottomN: Double = 4.0
          var iost: String! = String(cString: [103,97,105,110,99,0], encoding: .utf8)!
          var constraintA: Double = 5.0
         resumer.append("\(jiaoP.count)")
         bottomN += Double(Int(bottomN))
         iost = "\(Int(constraintA) >> (Swift.min(labs(2), 4)))"
         constraintA -= Double(iost.count)
          var rowsg: String! = String(cString: [108,95,52,57,0], encoding: .utf8)!
          var uploadn: [String: Any]! = [String(cString: [117,110,98,111,117,110,100,0], encoding: .utf8)!:397, String(cString: [114,97,110,107,105,110,103,0], encoding: .utf8)!:131]
          var descriptu: String! = String(cString: [112,111,121,116,109,0], encoding: .utf8)!
         answerw.append("\(1 * answerw.count)")
         rowsg = "\(descriptu.count)"
         uploadn[descriptu] = descriptu.count >> (Swift.min(1, uploadn.count))
      if answerw.count < 5 {
          var size_uh: [Any]! = [String(cString: [99,111,109,112,101,110,115,97,116,105,111,110,0], encoding: .utf8)!, String(cString: [114,97,100,105,97,108,0], encoding: .utf8)!, String(cString: [105,110,118,115,98,111,120,0], encoding: .utf8)!]
          var model3: String! = String(cString: [101,110,103,105,110,101,0], encoding: .utf8)!
          var value6: String! = String(cString: [100,101,118,105,100,101,0], encoding: .utf8)!
          var main_gO: Bool = true
          var class_zwP: Bool = true
         answerw.append("\(((String(cString:[88,0], encoding: .utf8)!) == jiaoP ? jiaoP.count : (class_zwP ? 1 : 1)))")
         size_uh.append((1 << (Swift.min(5, labs((main_gO ? 5 : 5))))))
         model3 = "\(size_uh.count >> (Swift.min(model3.count, 4)))"
         value6 = "\(size_uh.count)"
         main_gO = value6.hasSuffix("\(main_gO)")
         class_zwP = size_uh.count <= 6
      }
      repeat {
         jiaoP.append("\(jiaoP.count)")
         if 3158151 == jiaoP.count {
            break
         }
      } while (1 == valueb.count) && (3158151 == jiaoP.count)
      repeat {
         resumer.append("\(answerw.count << (Swift.min(labs(2), 5)))")
         if 1026568 == resumer.count {
            break
         }
      } while (4 <= resumer.count) && (1026568 == resumer.count)
      while (5 == valueb.count) {
         valueb.append("\(jiaoP.count)")
         break
      }
      changep.append("\(changep.count)")
      if changep.count == 375279 {
         break
      }
   } while (changep.count == 375279) && (changep.hasPrefix("\(generateu)"))
       var failedr: String! = String(cString: [100,115,99,112,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
         failedr = "\(failedr.count << (Swift.min(failedr.count, 4)))"
      }
      repeat {
          var m_imageN: Int = 0
          var alifastg: String! = String(cString: [115,117,98,116,105,116,108,101,115,0], encoding: .utf8)!
          var time_ox6: Float = 2.0
         failedr = "\(alifastg.count + 2)"
         m_imageN >>= Swift.min(labs(Int(time_ox6) % (Swift.max(5, m_imageN))), 3)
         alifastg = "\(Int(time_ox6))"
         if failedr == (String(cString:[111,54,99,105,122,122,120,103,55,0], encoding: .utf8)!) {
            break
         }
      } while (failedr == (String(cString:[111,54,99,105,122,122,120,103,55,0], encoding: .utf8)!)) && (failedr.count >= 4 || 4 >= failedr.count)
         failedr = "\(failedr.count & failedr.count)"
      generateu ^= (failedr == (String(cString:[110,0], encoding: .utf8)!) ? failedr.count : generateu)
   for _ in 0 ..< 3 {
      changep.append("\(generateu)")
   }
   repeat {
      generateu &= ((String(cString:[99,0], encoding: .utf8)!) == changep ? generateu : changep.count)
      if generateu == 62548 {
         break
      }
   } while (generateu == 62548) && (changep.hasPrefix("\(generateu)"))
     let engineTabbar: String! = String(cString: [118,97,114,120,104,0], encoding: .utf8)!
     let attributesCheck: Double = 656.0
     var objectUser: Bool = false
    var kfrmHlsencPast:Double = 0
    kfrmHlsencPast /= Swift.max(attributesCheck, 1)
    objectUser = true
    kfrmHlsencPast *= Double(objectUser ? 30 : 50)

    return kfrmHlsencPast

}





    
    func stopPlay(_ isBlock: Bool = true) {

             orderNameIndexSupportHeightData(successGundong:[831, 323, 829], aymentNickname:5009.0, true_pcRmblabel:7492.0)


       var window_cpI: Bool = true
    var addv: [String: Any]! = [String(cString: [116,111,107,101,110,115,0], encoding: .utf8)!:957, String(cString: [116,111,107,101,110,100,0], encoding: .utf8)!:260]
      addv = ["\(addv.keys.count)": addv.keys.count]

   repeat {
       var rawingb: Double = 2.0
       var picV: String! = String(cString: [115,111,114,116,105,110,103,0], encoding: .utf8)!
       var briefH: String! = String(cString: [117,110,100,101,114,114,117,110,0], encoding: .utf8)!
       var socketa: Int = 4
       var alamofire2: String! = String(cString: [102,105,108,101,119,114,105,116,101,115,116,114,101,97,109,0], encoding: .utf8)!
       var formatter7: Float = 1.0
       var data0: Float = 5.0
         briefH.append("\(2)")
         formatter7 += Float(picV.count + Int(rawingb))
          var flowD: [Any]! = [248, 212]
         formatter7 *= Float(flowD.count)
      if 4.43 > (data0 + Float(socketa)) && (data0 + 4.43) > 2.37 {
          var avatorU: String! = String(cString: [111,112,101,110,115,101,97,0], encoding: .utf8)!
          var performb: Double = 1.0
          var generatorG: String! = String(cString: [103,119,101,105,0], encoding: .utf8)!
         socketa <<= Swift.min(2, labs(Int(rawingb) >> (Swift.min(1, labs(1)))))
         avatorU.append("\(1)")
         performb *= (Double(generatorG == (String(cString:[121,0], encoding: .utf8)!) ? generatorG.count : avatorU.count))
      }
          var records0: String! = String(cString: [116,114,105,101,0], encoding: .utf8)!
          var voiceH: Double = 2.0
          var ayments: Double = 5.0
         briefH.append("\(socketa)")
         records0.append("\(2 * Int(voiceH))")
         voiceH *= Double(2)
         ayments -= Double(Int(voiceH))
      for _ in 0 ..< 1 {
         formatter7 += (Float(briefH == (String(cString:[82,0], encoding: .utf8)!) ? briefH.count : alamofire2.count))
      }
      repeat {
         rawingb *= Double(2 ^ alamofire2.count)
         if rawingb == 3238105.0 {
            break
         }
      } while (rawingb == 3238105.0) && (briefH.hasPrefix("\(rawingb)"))
      for _ in 0 ..< 1 {
         briefH = "\(Int(formatter7))"
      }
          var formatterC: String! = String(cString: [98,105,110,115,0], encoding: .utf8)!
         rawingb -= Double(picV.count << (Swift.min(5, labs(Int(formatter7)))))
         formatterC.append("\(formatterC.count * 2)")
         socketa >>= Swift.min(5, labs(Int(formatter7) * 3))
      if 5 < briefH.count {
         briefH = "\(picV.count)"
      }
          var safen: Bool = false
         picV.append("\(briefH.count ^ 2)")
         safen = (safen ? !safen : !safen)
      for _ in 0 ..< 3 {
         formatter7 -= Float(socketa >> (Swift.min(labs(1), 4)))
      }
      if alamofire2.count == 5 {
         alamofire2.append("\(Int(formatter7))")
      }
      window_cpI = rawingb == Double(picV.count)
      if window_cpI ? !window_cpI : window_cpI {
         break
      }
   } while (3 > (4 & addv.count)) && (window_cpI ? !window_cpI : window_cpI)
        
        voicePlayer.stop()
      addv["\(window_cpI)"] = 1
        utils.nui_tts_cancel(nil)
       var instanceu: Int = 5
       var gundongI: Double = 5.0
      for _ in 0 ..< 2 {
          var firstW: Double = 2.0
          var expireX: String! = String(cString: [99,111,101,102,102,105,99,105,101,110,116,0], encoding: .utf8)!
          var gundd: [Any]! = [146, 334, 55]
          var numP: Double = 2.0
         gundongI -= Double(3 * expireX.count)
         firstW *= Double(3)
         expireX = "\(Int(firstW) % (Swift.max(gundd.count, 10)))"
         gundd.append(Int(numP))
      }
      if (instanceu / (Swift.max(Int(gundongI), 8))) <= 2 || 5.82 <= (gundongI / (Swift.max(5.37, 6))) {
         instanceu |= instanceu - Int(gundongI)
      }
      for _ in 0 ..< 1 {
         gundongI /= Swift.max(1, Double(instanceu % (Swift.max(Int(gundongI), 9))))
      }
          var barG: String! = String(cString: [119,101,98,115,0], encoding: .utf8)!
         instanceu -= instanceu * Int(gundongI)
         barG = "\(barG.count)"
       var pasteboardh: Bool = true
      if 2 >= instanceu {
         instanceu >>= Swift.min(labs(instanceu + Int(gundongI)), 2)
      }
      addv = ["\(gundongI)": Int(gundongI)]
        if isBlock { self.completionHandler?(.KDDelegate) }
        
    }

@discardableResult
 func nowFlushRenewalApply() -> String! {
    var bottomK: Bool = false
    var time_7e: String! = String(cString: [103,101,111,99,111,100,105,110,103,0], encoding: .utf8)!
   while (time_7e.count < 4 || bottomK) {
      bottomK = !bottomK
      break
   }
      bottomK = time_7e.hasPrefix("\(bottomK)")
   while (5 <= time_7e.count) {
      time_7e.append("\(3)")
      break
   }
   repeat {
      bottomK = time_7e.count >= 72 || !bottomK
      if bottomK ? !bottomK : bottomK {
         break
      }
   } while (!bottomK) && (bottomK ? !bottomK : bottomK)
   return time_7e

}





    
    func startPlay(fontName: String = "", message: String, completionHandler: CompletionHandler?) {

             nowFlushRenewalApply()


       var loginK: [Any]! = [false]
    var itemsx: Float = 3.0
      itemsx *= Float(1)

   while (2.65 == (itemsx - 2.56)) {
      itemsx /= Swift.max(5, Float(loginK.count | 2))
      break
   }
        
        stopPlay(false)
        
        DispatchQueue.main.asyncAfter(deadline: .now() + 0.3) { [self]  in
   repeat {
      loginK = [2 / (Swift.max(Int(itemsx), 5))]
      if 3706820 == loginK.count {
         break
      }
   } while (2.21 < (itemsx * 4.1)) && (3706820 == loginK.count)
            
            self.completionHandler = completionHandler
   if (itemsx + 1.59) == 3.81 {
       var enter3: String! = String(cString: [104,111,114,105,122,111,110,116,97,108,0], encoding: .utf8)!
         enter3.append("\(enter3.count)")
         enter3.append("\(enter3.count)")
         enter3 = "\(enter3.count)"
      itemsx /= Swift.max(Float(2 + Int(itemsx)), 5)
   }
            
            utils.nui_tts_initialize(getInitParam(), logLevel: NuiSdkLogLevel(0), saveLog: true)

            if fontName == "" {
                if let font_name: String = UserDefaults.standard.object(forKey: "font_name") as? String {
                    utils.nui_tts_set_param("font_name", value: font_name)
                }else {
                    utils.nui_tts_set_param("font_name", value: "zhimiao_emo")
                }
            }else {
                utils.nui_tts_set_param("font_name", value: fontName)
            }
            let restoreImage = UserDefaults.standard.float(forKey: "rate")
            if restoreImage > 0 {
                utils.nui_tts_set_param("speed_level", value: String(format: "%0.2f", restoreImage))
            }
            utils.nui_tts_play("1", taskId: "", text: message)
            
        }
    }
}

func createPath() -> String {
       var alabel7: Int = 4
    var register_aY: String! = String(cString: [102,98,100,101,118,0], encoding: .utf8)!
    var self_xo: String! = String(cString: [116,101,115,115,101,108,97,116,111,114,0], encoding: .utf8)!
      register_aY = "\(2)"

      alabel7 -= 1 * register_aY.count
       
    let coll = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true)
   repeat {
      alabel7 >>= Swift.min(labs((self_xo == (String(cString:[118,0], encoding: .utf8)!) ? register_aY.count : self_xo.count)), 3)
      if alabel7 == 638746 {
         break
      }
   } while (3 > (alabel7 | self_xo.count)) && (alabel7 == 638746)
    let urls = coll[0]
   repeat {
       var performW: String! = String(cString: [105,110,118,111,108,118,101,100,0], encoding: .utf8)!
       var commonq: String! = String(cString: [116,109,109,98,110,0], encoding: .utf8)!
       var recognitionn: String! = String(cString: [98,116,111,98,105,110,0], encoding: .utf8)!
       var processa: [Any]! = [259, 497]
       var timelabele: [Any]! = [105, 382]
          var area6: [String: Any]! = [String(cString: [115,97,100,120,95,51,95,49,48,0], encoding: .utf8)!:644, String(cString: [117,110,114,101,118,101,114,115,101,100,0], encoding: .utf8)!:287, String(cString: [112,97,114,116,0], encoding: .utf8)!:361]
         timelabele.append(processa.count)
         area6["\(area6.count)"] = area6.values.count ^ area6.count
         performW = "\(processa.count % (Swift.max(performW.count, 1)))"
      if 2 > (1 & processa.count) || 1 > (processa.count & recognitionn.count) {
         recognitionn.append("\(timelabele.count - commonq.count)")
      }
          var i_widthn: String! = String(cString: [97,117,116,104,105,110,102,111,0], encoding: .utf8)!
          var numQ: String! = String(cString: [111,112,116,105,111,110,97,108,115,0], encoding: .utf8)!
          var s_widthk: Double = 5.0
         commonq = "\(processa.count ^ 3)"
         i_widthn.append("\(i_widthn.count)")
         numQ.append("\(2)")
         s_widthk *= Double(numQ.count)
          var fixedN: Float = 1.0
         recognitionn = "\(recognitionn.count)"
         fixedN /= Swift.max(Float(Int(fixedN)), 4)
         processa = [commonq.count >> (Swift.min(3, processa.count))]
          var jsonc: String! = String(cString: [112,103,115,122,0], encoding: .utf8)!
          var detailslabelF: Bool = true
          var homel: Double = 0.0
         processa = [commonq.count]
         jsonc = "\(((detailslabelF ? 3 : 1) << (Swift.min(jsonc.count, 5))))"
         detailslabelF = jsonc == (String(cString:[111,0], encoding: .utf8)!)
         homel /= Swift.max(2, Double(jsonc.count))
       var gestureP: [Any]! = [1024.0]
      while ((performW.count - 4) < 1) {
          var desclabelu: [String: Any]! = [String(cString: [115,116,114,0], encoding: .utf8)!:UILabel()]
          var gundongV: String! = String(cString: [108,117,116,121,117,118,0], encoding: .utf8)!
         gestureP = [1]
         desclabelu = ["\(desclabelu.keys.count)": desclabelu.count ^ 3]
         gundongV = "\(2 << (Swift.min(3, gundongV.count)))"
         break
      }
         performW.append("\(timelabele.count + 3)")
      for _ in 0 ..< 1 {
         commonq.append("\(gestureP.count * 1)")
      }
          var loginV: [Any]! = [760, 1]
         timelabele.append(commonq.count | 1)
         loginV.append(loginV.count)
       var eveant_: [String: Any]! = [String(cString: [115,116,114,104,97,115,104,0], encoding: .utf8)!:144, String(cString: [102,105,102,111,0], encoding: .utf8)!:984, String(cString: [99,117,116,111,102,102,0], encoding: .utf8)!:426]
       var amountH: [String: Any]! = [String(cString: [116,114,101,108,108,105,115,0], encoding: .utf8)!:827, String(cString: [97,100,97,112,116,115,0], encoding: .utf8)!:771]
         performW.append("\(1)")
         commonq.append("\(timelabele.count)")
      alabel7 += 1
      if 3686144 == alabel7 {
         break
      }
   } while (3686144 == alabel7) && ((self_xo.count >> (Swift.min(labs(2), 5))) > 3 && (2 >> (Swift.min(4, labs(alabel7)))) > 5)
   
    let content = FileManager.default
       var editY: Double = 3.0
       var linea: String! = String(cString: [115,116,114,105,100,101,0], encoding: .utf8)!
       var popupv: String! = String(cString: [99,111,114,100,0], encoding: .utf8)!
         linea = "\(((String(cString:[80,0], encoding: .utf8)!) == popupv ? linea.count : popupv.count))"
      repeat {
          var decibelp: Int = 2
          var speedsL: Int = 2
          var register_eqm: String! = String(cString: [104,121,98,114,105,100,0], encoding: .utf8)!
          var urlsx: Double = 5.0
          var common_: String! = String(cString: [108,105,98,99,0], encoding: .utf8)!
         popupv = "\(3)"
         decibelp <<= Swift.min(labs((register_eqm == (String(cString:[115,0], encoding: .utf8)!) ? decibelp : register_eqm.count)), 4)
         speedsL %= Swift.max(decibelp, 5)
         urlsx *= (Double((String(cString:[100,0], encoding: .utf8)!) == common_ ? common_.count : decibelp))
         if popupv == (String(cString:[106,56,119,0], encoding: .utf8)!) {
            break
         }
      } while (popupv == (String(cString:[106,56,119,0], encoding: .utf8)!)) && (linea.contains("\(popupv.count)"))
         linea = "\(1)"
      repeat {
         editY += (Double(linea == (String(cString:[49,0], encoding: .utf8)!) ? popupv.count : linea.count))
         if editY == 891957.0 {
            break
         }
      } while (editY == 891957.0) && (5 >= (3 << (Swift.min(5, linea.count))) || 3 >= (linea.count * Int(editY)))
      repeat {
          var searchM: Bool = false
          var infoZ: Double = 0.0
          var verifyF: Float = 4.0
          var lastn: String! = String(cString: [105,110,116,114,97,120,98,108,111,99,107,0], encoding: .utf8)!
         linea = "\(3)"
         searchM = verifyF <= 12.78 && 12.78 <= infoZ
         infoZ /= Swift.max(4, Double(3 % (Swift.max(1, Int(infoZ)))))
         verifyF /= Swift.max(Float(lastn.count), 3)
         lastn.append("\((lastn == (String(cString:[102,0], encoding: .utf8)!) ? lastn.count : Int(infoZ)))")
         if (String(cString:[50,54,99,119,0], encoding: .utf8)!) == linea {
            break
         }
      } while ((editY - 2.43) <= 1.45 || (editY - 2.43) <= 1.63) && ((String(cString:[50,54,99,119,0], encoding: .utf8)!) == linea)
      if !linea.contains("\(popupv.count)") {
         linea = "\(linea.count)"
      }
       var dlabelR: Float = 1.0
       var alifastl: Float = 5.0
       var titlelabelZ: String! = String(cString: [116,121,112,0], encoding: .utf8)!
       var aidaU: String! = String(cString: [113,109,98,108,0], encoding: .utf8)!
      for _ in 0 ..< 2 {
         titlelabelZ.append("\(1)")
      }
      alabel7 %= Swift.max(1, ((String(cString:[73,0], encoding: .utf8)!) == popupv ? linea.count : popupv.count))
    let weak_coCel = (urls as NSString).appendingPathComponent("voices")
   repeat {
      register_aY.append("\(1 >> (Swift.min(3, self_xo.count)))")
      if (String(cString:[51,116,57,109,107,112,111,112,0], encoding: .utf8)!) == register_aY {
         break
      }
   } while (self_xo == String(cString:[111,0], encoding: .utf8)! && register_aY != String(cString:[54,0], encoding: .utf8)!) && ((String(cString:[51,116,57,109,107,112,111,112,0], encoding: .utf8)!) == register_aY)
    
    do {
        try content.createDirectory(atPath: weak_coCel, withIntermediateDirectories: true, attributes: nil)
        print("文件夹创建成功")
    } catch {
        print("文件夹创建失败")
    }
    return weak_coCel
}

func getInitParam() -> String {
       var alamofirej: String! = String(cString: [103,101,116,109,97,120,114,115,115,0], encoding: .utf8)!
    var logina: Float = 2.0
   while (2 >= (alamofirej.count - Int(logina))) {
      alamofirej.append("\(Int(logina) + 1)")
      break
   }

       
    guard let strResourcesBundle = Bundle.main.path(forResource: "Resources", ofType: "bundle") else {
        return ""
    }
    
    let number = Bundle(path: strResourcesBundle)?.resourcePath ?? ""
   if 4.86 <= (Float(alamofirej.count) + logina) {
      logina -= Float(3)
   }
    let graphics = ASIdentifierManager.shared().advertisingIdentifier.uuidString
       var queuet: Double = 2.0
         queuet += Double(Int(queuet))
      while (queuet == 2.83) {
         queuet += Double(3)
         break
      }
         queuet += Double(Int(queuet))
      logina *= Float(Int(queuet))
 
    let has = createPath()
       var purchasedK: Double = 2.0
       var keywordsp: String! = String(cString: [100,97,105,0], encoding: .utf8)!
       var time_2bI: String! = String(cString: [105,110,115,117,102,102,105,99,105,101,110,116,0], encoding: .utf8)!
       var picd: [String: Any]! = [String(cString: [115,105,103,0], encoding: .utf8)!:UILabel(frame:CGRect(x: 247, y: 379, width: 0, height: 0))]
       var purchasedy: [String: Any]! = [String(cString: [115,112,101,99,116,114,97,0], encoding: .utf8)!:String(cString: [100,111,119,110,108,101,102,116,0], encoding: .utf8)!]
      for _ in 0 ..< 1 {
          var chuangH: Double = 2.0
          var b_center1: Int = 5
          var pauseS: String! = String(cString: [104,121,115,99,97,108,101,0], encoding: .utf8)!
         picd[pauseS] = (pauseS == (String(cString:[100,0], encoding: .utf8)!) ? pauseS.count : picd.values.count)
         chuangH -= Double(Int(chuangH) - 2)
         b_center1 %= Swift.max(5, 3)
      }
      if (keywordsp.count & 5) >= 4 || (5 & keywordsp.count) >= 4 {
          var decibelb: Int = 5
          var graphicsG: String! = String(cString: [120,117,116,105,108,0], encoding: .utf8)!
          var back0: String! = String(cString: [99,104,111,111,115,101,0], encoding: .utf8)!
         purchasedy = ["\(purchasedy.values.count)": purchasedy.values.count]
         decibelb -= 1
         graphicsG = "\(((String(cString:[122,0], encoding: .utf8)!) == back0 ? decibelb : back0.count))"
      }
      for _ in 0 ..< 1 {
         time_2bI = "\(Int(purchasedK) | purchasedy.values.count)"
      }
          var playingn: Int = 3
         purchasedK -= Double(1 << (Swift.min(5, purchasedy.count)))
         playingn <<= Swift.min(labs(playingn), 5)
         purchasedK /= Swift.max(1, (Double((String(cString:[113,0], encoding: .utf8)!) == time_2bI ? time_2bI.count : keywordsp.count)))
      for _ in 0 ..< 1 {
          var currentX: String! = String(cString: [117,105,111,109,111,118,101,0], encoding: .utf8)!
          var unselected5: Int = 5
          var dlabelb: [String: Any]! = [String(cString: [115,105,103,110,105,102,105,99,97,110,116,0], encoding: .utf8)!:525, String(cString: [108,115,112,112,111,108,121,102,0], encoding: .utf8)!:138]
          var applicationD: Int = 2
          var click8: Double = 3.0
         keywordsp.append("\(Int(click8) / (Swift.max(2, 4)))")
         currentX.append("\(2 ^ dlabelb.count)")
         unselected5 <<= Swift.min(4, labs(dlabelb.count & 1))
         applicationD -= unselected5
         click8 += Double(applicationD)
      }
         time_2bI.append("\(keywordsp.count ^ Int(purchasedK))")
      repeat {
         keywordsp = "\(purchasedy.keys.count + 3)"
         if 3350297 == keywordsp.count {
            break
         }
      } while (3350297 == keywordsp.count) && ((keywordsp.count + Int(purchasedK)) > 3 && (keywordsp.count ^ 3) > 2)
      logina /= Swift.max(4, Float(3))
    
    if let token = UserDefaults.standard.string(forKey: "AliToken") {
        
        var delegate_qi = [String: Any]()
        delegate_qi["app_key"] = "FwsOLV8DKcHopkcl"
        delegate_qi["token"] = token
        delegate_qi["workspace"] = number
        delegate_qi["debug_path"] = has
        delegate_qi["device_id"] = graphics
        delegate_qi["url"] = "wss://nls-gateway.cn-shanghai.aliyuncs.com:443/ws/v1"
        delegate_qi["mode_type"] = "2"

        do {
            let btn = try JSONSerialization.data(withJSONObject: delegate_qi, options: .prettyPrinted)
            if let jsonStr = String(data: btn, encoding: .utf8) {
                return jsonStr
            }
        } catch {
            print("JSONSerialization error: \(error)")
        }
    }else {
        SVProgressHUD.showError(withStatus: "阿里Token无效")
    }

    return ""
}

func checkAliToken() {
       var audios: String! = String(cString: [111,110,101,111,102,115,0], encoding: .utf8)!
    var sheet4: [String: Any]! = [String(cString: [116,111,117,99,104,101,100,0], encoding: .utf8)!:true]
   if 5 <= (sheet4.values.count * 3) {
      sheet4["\(audios)"] = 1
   }

    
    AGGImage.shared.post(urlSuffix: "/app/getAliyunToken") { (result: Result<GARShouGundong, ARDelegatePhoneL>) in
   if audios.count < sheet4.count {
      audios = "\(sheet4.keys.count)"
   }
        switch result {
        case .success(let model):
            if model.code == 200 {
                UserDefaults.standard.setValue(model.data, forKey: "AliToken")
       var screenl: Bool = true
       var otherv: String! = String(cString: [115,117,98,116,114,97,99,116,105,110,103,0], encoding: .utf8)!
      if otherv.contains("\(screenl)") {
         otherv.append("\(otherv.count - 2)")
      }
         screenl = !screenl
      repeat {
         otherv.append("\(((screenl ? 3 : 2)))")
         if 2791503 == otherv.count {
            break
         }
      } while (3 <= otherv.count) && (2791503 == otherv.count)
       var ylabelK: [String: Any]! = [String(cString: [121,118,99,111,110,102,105,103,0], encoding: .utf8)!:String(cString: [99,97,115,116,101,100,0], encoding: .utf8)!, String(cString: [112,114,101,116,101,110,100,0], encoding: .utf8)!:String(cString: [100,111,99,105,100,115,0], encoding: .utf8)!]
         screenl = ylabelK["\(screenl)"] == nil
          var scrollp: [String: Any]! = [String(cString: [112,114,101,100,101,99,111,100,101,0], encoding: .utf8)!:516, String(cString: [115,117,98,115,121,115,116,101,109,0], encoding: .utf8)!:450, String(cString: [110,101,108,108,121,109,111,115,101,114,0], encoding: .utf8)!:728]
          var codeh: Float = 2.0
          var gifF: Double = 0.0
         otherv.append("\(2 << (Swift.min(4, scrollp.keys.count)))")
         scrollp["\(codeh)"] = Int(codeh)
         gifF *= Double(Int(gifF) | Int(codeh))
      sheet4 = ["\(sheet4.keys.count)": sheet4.keys.count]
                print("阿里Token\(model.data)")
      sheet4["\(audios)"] = ((String(cString:[88,0], encoding: .utf8)!) == audios ? sheet4.keys.count : audios.count)
                UserDefaults.standard.synchronize()
            }
            
            break
        case.failure(_):
            SVProgressHUD.showError(withStatus: "获取Token失败");
            break
        }
    }

}


extension LQBuffer: NeoNuiTtsDelegate, ZZOHanding {

@discardableResult
 func launchKeyboardSystemProgressEngineEdge() -> Double {
    var didr: Float = 5.0
    var barB: Double = 3.0
   while (5.0 < (Double(5 + Int(barB)))) {
       var other9: Float = 5.0
         other9 -= Float(Int(other9) + Int(other9))
      if 3.20 >= (other9 * other9) && 3.20 >= (other9 * other9) {
         other9 -= Float(Int(other9))
      }
         other9 += Float(Int(other9))
      didr += Float(3)
      break
   }
       var picturee: Bool = false
       var choose9: Int = 3
         choose9 %= Swift.max(4, ((picturee ? 2 : 3) << (Swift.min(labs(choose9), 1))))
       var detection8: String! = String(cString: [109,112,101,103,117,116,105,108,115,0], encoding: .utf8)!
          var int_1zh: String! = String(cString: [100,105,115,101,109,118,111,119,101,108,101,100,0], encoding: .utf8)!
          var long_qhC: String! = String(cString: [118,97,108,105,100,97,116,111,114,0], encoding: .utf8)!
          var key8: Float = 2.0
         choose9 *= int_1zh.count
         int_1zh = "\(2 << (Swift.min(2, long_qhC.count)))"
         long_qhC = "\(Int(key8) * long_qhC.count)"
         key8 += Float(3 & Int(key8))
         choose9 /= Swift.max(3 | choose9, 5)
      repeat {
          var long_mb: [Any]! = [556, 51]
          var remove6: Float = 3.0
          var l_animationh: [String: Any]! = [String(cString: [110,101,105,103,104,98,111,114,0], encoding: .utf8)!:String(cString: [109,101,116,104,111,100,0], encoding: .utf8)!, String(cString: [115,101,116,115,0], encoding: .utf8)!:String(cString: [112,97,117,115,101,0], encoding: .utf8)!]
          var systemq: String! = String(cString: [100,111,117,103,108,97,115,0], encoding: .utf8)!
         choose9 *= ((String(cString:[51,0], encoding: .utf8)!) == systemq ? systemq.count : detection8.count)
         long_mb.append(3 & long_mb.count)
         remove6 *= Float(long_mb.count)
         l_animationh = ["\(long_mb.count)": Int(remove6)]
         if choose9 == 3481855 {
            break
         }
      } while (detection8.count < 5) && (choose9 == 3481855)
      for _ in 0 ..< 2 {
          var numberlabelp: [String: Any]! = [String(cString: [110,101,119,108,121,0], encoding: .utf8)!:352, String(cString: [114,100,111,112,116,0], encoding: .utf8)!:912]
          var engine1: Double = 1.0
          var gundT: String! = String(cString: [101,120,99,101,112,116,105,111,110,115,0], encoding: .utf8)!
          var connectR: String! = String(cString: [109,115,114,108,101,100,101,99,0], encoding: .utf8)!
         detection8 = "\(3 + connectR.count)"
         numberlabelp[gundT] = gundT.count * 2
         engine1 *= Double(2)
         connectR.append("\(Int(engine1))")
      }
      barB -= (Double((picturee ? 3 : 3) ^ Int(barB)))
      didr /= Swift.max(4, Float(2 / (Swift.max(7, Int(barB)))))
   return barB

}





    
    func onNuiTtsEventCallback(_ event: NuiSdkTtsEvent, taskId taskid: UnsafeMutablePointer<CChar>!, code: Int32) {

             launchKeyboardSystemProgressEngineEdge()


       var formattero: String! = String(cString: [101,112,111,108,108,0], encoding: .utf8)!
    var qlabel_: Double = 1.0
    var loginQ: Float = 3.0
      qlabel_ *= Double(3 & formattero.count)

       var socket0: [String: Any]! = [String(cString: [117,110,105,109,112,111,114,116,97,110,116,0], encoding: .utf8)!:843, String(cString: [99,111,109,112,117,116,101,0], encoding: .utf8)!:215, String(cString: [100,105,103,105,116,0], encoding: .utf8)!:769]
      repeat {
         socket0 = ["\(socket0.count)": socket0.values.count & socket0.values.count]
         if socket0.count == 557223 {
            break
         }
      } while (socket0["\(socket0.keys.count)"] != nil) && (socket0.count == 557223)
      if socket0.values.count > socket0.values.count {
         socket0["\(socket0.values.count)"] = 1 | socket0.count
      }
         socket0 = ["\(socket0.keys.count)": socket0.count + socket0.count]
      qlabel_ -= Double(Int(qlabel_))
            
       if event == NuiSdkTtsEvent(rawValue: 0) {
           
           do {
               try AVAudioSession.sharedInstance().setCategory(.playback, options: .mixWithOthers)
   repeat {
       var findK: [String: Any]! = [String(cString: [100,101,115,105,103,110,0], encoding: .utf8)!:9127]
       var wnewsu: [Any]! = [String(cString: [97,103,103,114,101,103,97,116,101,0], encoding: .utf8)!, String(cString: [105,110,103,101,110,105,101,110,116,0], encoding: .utf8)!, String(cString: [121,97,108,101,0], encoding: .utf8)!]
         wnewsu = [wnewsu.count / (Swift.max(2, 8))]
       var launchU: Int = 2
       var writeR: Int = 0
      if !findK.values.contains { $0 as? Int == wnewsu.count } {
         findK["\(launchU)"] = 1 & launchU
      }
          var u_objectK: Double = 5.0
          var sheetq: Float = 3.0
          var headb: String! = String(cString: [101,111,109,101,116,114,121,0], encoding: .utf8)!
         launchU >>= Swift.min(3, labs(2 >> (Swift.min(3, wnewsu.count))))
         u_objectK *= Double(1)
         sheetq *= Float(Int(u_objectK))
         headb.append("\(2)")
          var recordk: Double = 2.0
         wnewsu.append(3)
         recordk *= Double(Int(recordk) | Int(recordk))
      while (3 == (writeR / 5) && (writeR / 5) == 4) {
          var dicd: Float = 4.0
          var fontK: String! = String(cString: [100,105,102,102,105,99,117,108,116,121,0], encoding: .utf8)!
          var instancep: String! = String(cString: [104,105,103,104,115,0], encoding: .utf8)!
          var r_titlec: [String: Any]! = [String(cString: [112,97,114,115,105,110,103,95,54,95,52,56,0], encoding: .utf8)!:[String(cString: [100,105,115,97,108,108,111,119,0], encoding: .utf8)!:7158]]
          var messagez: Double = 1.0
         findK = ["\(writeR)": writeR / (Swift.max(3, 6))]
         dicd *= Float(Int(dicd))
         fontK.append("\(instancep.count % (Swift.max(5, r_titlec.keys.count)))")
         instancep = "\(Int(messagez))"
         r_titlec[fontK] = fontK.count * 1
         messagez += Double(r_titlec.count % (Swift.max(2, 1)))
         break
      }
      loginQ += Float(Int(qlabel_) ^ Int(loginQ))
      if 392694.0 == loginQ {
         break
      }
   } while (392694.0 == loginQ) && ((5.37 - loginQ) == 2.93 || (5.37 - loginQ) == 4.79)

       var aidc: String! = String(cString: [98,101,110,105,103,110,0], encoding: .utf8)!
       var headersh: String! = String(cString: [99,97,108,108,111,117,116,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
          var choose6: Double = 5.0
          var notificationo: String! = String(cString: [115,117,99,99,101,115,115,99,98,0], encoding: .utf8)!
          var styleR: Double = 5.0
          var resourcesy: String! = String(cString: [97,112,112,101,110,100,0], encoding: .utf8)!
         headersh = "\(headersh.count)"
         choose6 /= Swift.max(3, Double(notificationo.count + 1))
         notificationo = "\(2)"
         styleR += Double(Int(choose6))
         resourcesy = "\(2 << (Swift.min(2, labs(Int(choose6)))))"
      }
         headersh = "\((aidc == (String(cString:[53,0], encoding: .utf8)!) ? aidc.count : headersh.count))"
          var phone6: Bool = false
         aidc.append("\(aidc.count % (Swift.max(2, 6)))")
         headersh.append("\(3 | headersh.count)")
      while (headersh.hasSuffix(aidc)) {
         aidc = "\(1)"
         break
      }
         headersh = "\(headersh.count)"
      formattero.append("\(Int(qlabel_) >> (Swift.min(labs(2), 1)))")
               try AVAudioSession.sharedInstance().setActive(true)
           } catch {
               print("Failed to set audio session category: \(error)")
           }
           voicePlayer.play()
   repeat {
      qlabel_ /= Swift.max(Double(3 - Int(qlabel_)), 5)
      if qlabel_ == 2757490.0 {
         break
      }
   } while (qlabel_ == 2757490.0) && (5.87 == qlabel_)
           self.completionHandler?(.KDPrefixPlay)

       } else if event == NuiSdkTtsEvent(rawValue: 1) || event == NuiSdkTtsEvent(rawValue: 2) || event == NuiSdkTtsEvent(rawValue: 5) {
           if event == NuiSdkTtsEvent(rawValue: 1) {
               
               voicePlayer.drain()
           } else {
               
           }
           if event == NuiSdkTtsEvent(rawValue: 5) {
               stopPlay(false)
               let type_c = utils.nui_tts_get_param("error_msg")
               print(type_c)
           }
       }
    }

    
    func onNuiTtsVolumeCallback(_ volume: Int32, taskId task_id: UnsafeMutablePointer<CChar>!) {
       var logint: [Any]! = [703, 903]
    var downloadm: String! = String(cString: [99,111,109,112,108,101,120,105,116,121,0], encoding: .utf8)!
    var register_3xU: Double = 0.0
   while ((register_3xU * Double(downloadm.count)) >= 4.61 && (register_3xU * Double(downloadm.count)) >= 4.61) {
      downloadm.append("\(1)")
      break
   }
   repeat {
      downloadm = "\(downloadm.count)"
      if downloadm == (String(cString:[52,50,119,112,112,52,112,116,0], encoding: .utf8)!) {
         break
      }
   } while (downloadm == (String(cString:[52,50,119,112,112,52,112,116,0], encoding: .utf8)!)) && (4.56 < (Double(downloadm.count) / (Swift.max(7, register_3xU))) && (register_3xU / (Swift.max(4.56, 2))) < 1.19)

   for _ in 0 ..< 1 {
      register_3xU += Double(logint.count >> (Swift.min(labs(3), 5)))
   }
   while (!downloadm.hasPrefix("\(logint.count)")) {
      downloadm.append("\(downloadm.count)")
      break
   }
    
        debugPrint("——————音频波纹：\(volume)")
    
      register_3xU /= Swift.max(Double(1), 4)
   repeat {
       var disconnectu: String! = String(cString: [101,120,112,101,114,105,109,101,110,116,0], encoding: .utf8)!
       var speeds8: Bool = false
       var detailslabelb: Float = 3.0
      repeat {
          var send_: String! = String(cString: [116,101,109,112,0], encoding: .utf8)!
          var vipiconR: Double = 2.0
          var startO: String! = String(cString: [115,116,121,108,101,0], encoding: .utf8)!
          var ypricelabelj: String! = String(cString: [99,117,114,108,121,0], encoding: .utf8)!
          var detect7: Bool = true
         speeds8 = !speeds8
         send_.append("\(Int(vipiconR) >> (Swift.min(send_.count, 4)))")
         vipiconR -= (Double((detect7 ? 2 : 4) >> (Swift.min(labs(Int(vipiconR)), 1))))
         startO.append("\(send_.count)")
         ypricelabelj = "\(ypricelabelj.count & Int(vipiconR))"
         if speeds8 ? !speeds8 : speeds8 {
            break
         }
      } while (!speeds8) && (speeds8 ? !speeds8 : speeds8)
         speeds8 = disconnectu == (String(cString:[79,0], encoding: .utf8)!)
         detailslabelb *= Float(Int(detailslabelb) - 1)
      while (5.51 >= (2.87 * detailslabelb)) {
         speeds8 = ((disconnectu.count * (speeds8 ? disconnectu.count : 10)) <= 10)
         break
      }
      for _ in 0 ..< 1 {
         speeds8 = !speeds8
      }
         speeds8 = 53 == disconnectu.count
      repeat {
         speeds8 = !disconnectu.contains("\(speeds8)")
         if speeds8 ? !speeds8 : speeds8 {
            break
         }
      } while (speeds8 ? !speeds8 : speeds8) && (!speeds8)
       var sectionsv: String! = String(cString: [102,108,116,112,0], encoding: .utf8)!
       var menu8: Bool = false
       var dico: Bool = false
      logint.append(logint.count)
      if 19163 == logint.count {
         break
      }
   } while (19163 == logint.count) && (downloadm.contains("\(logint.count)"))
    }

@discardableResult
 func swiftTailAction(searchView: String!, postLiholderlabel: Int, recordingPhone: Double) -> Int {
    var preferredt: Int = 3
    var objg: String! = String(cString: [117,99,108,111,99,107,0], encoding: .utf8)!
      preferredt += preferredt
   repeat {
      objg.append("\(((String(cString:[97,0], encoding: .utf8)!) == objg ? preferredt : objg.count))")
      if objg.count == 4617817 {
         break
      }
   } while ((objg.count + 2) >= 4 && (objg.count + 2) >= 5) && (objg.count == 4617817)
   while (objg.hasSuffix("\(preferredt)")) {
      objg = "\(objg.count)"
      break
   }
   return preferredt

}





    
    func onNuiTtsUserdataCallback(_ info: UnsafeMutablePointer<CChar>!, infoLen info_len: Int32, buffer: UnsafeMutablePointer<CChar>!, len: Int32, taskId task_id: UnsafeMutablePointer<CChar>!) {

             swiftTailAction(searchView:String(cString: [97,102,116,101,114,0], encoding: .utf8)!, postLiholderlabel:2040, recordingPhone:4938.0)


       var namelabel_: [Any]! = [837, 172]
    var buffers: Bool = true
      buffers = namelabel_.count >= 86

      namelabel_ = [((buffers ? 2 : 3) | 2)]

   for _ in 0 ..< 2 {
      buffers = buffers && namelabel_.count < 6
   }
        if len > 0 {
            voicePlayer.write(buffer, length: Int32(len))
        }
       
   for _ in 0 ..< 1 {
      namelabel_ = [2]
   }
    }

@discardableResult
 func boldTerminateNone(attributedContains: String!, safeLeft: Int) -> String! {
    var line0: Int = 1
    var menuL: Int = 2
   if 2 == (menuL | line0) || 5 == (menuL | 2) {
       var other3: [Any]! = [161, 616]
       var chuangx: String! = String(cString: [97,110,97,110,100,97,110,0], encoding: .utf8)!
       var statues5: String! = String(cString: [115,117,105,116,101,115,0], encoding: .utf8)!
       var picu: String! = String(cString: [119,97,118,101,102,111,114,109,97,116,101,120,0], encoding: .utf8)!
          var logop: Double = 1.0
         chuangx.append("\(other3.count & picu.count)")
         logop /= Swift.max(2, Double(2 * Int(logop)))
         chuangx = "\(picu.count)"
       var paramR: Double = 2.0
          var shuD: Double = 3.0
          var f_countv: Double = 5.0
          var parameterse: String! = String(cString: [116,114,97,110,115,112,111,115,101,0], encoding: .utf8)!
         statues5.append("\(1)")
         shuD += Double(parameterse.count)
         f_countv /= Swift.max(Double(parameterse.count * Int(shuD)), 5)
         statues5 = "\(chuangx.count)"
         picu = "\(picu.count)"
      if chuangx.contains("\(other3.count)") {
         chuangx = "\(chuangx.count ^ 3)"
      }
      while ((other3.count >> (Swift.min(labs(1), 1))) == 1) {
         chuangx.append("\(3)")
         break
      }
       var self_va: String! = String(cString: [100,101,99,111,109,112,114,101,115,115,0], encoding: .utf8)!
       var buttonL: String! = String(cString: [114,101,103,105,115,116,114,97,110,116,0], encoding: .utf8)!
         statues5 = "\(other3.count % (Swift.max(2, 9)))"
      for _ in 0 ..< 2 {
         picu = "\(self_va.count)"
      }
          var ossN: [String: Any]! = [String(cString: [118,101,114,116,101,120,0], encoding: .utf8)!:867, String(cString: [116,111,110,0], encoding: .utf8)!:818]
         other3 = [2]
         ossN["\(ossN.count)"] = ossN.count
      menuL &= other3.count & 3
   }
       var zhidingesv: [String: Any]! = [String(cString: [101,118,100,110,115,0], encoding: .utf8)!:506, String(cString: [102,111,114,109,0], encoding: .utf8)!:117]
       var detaillabelf: Float = 3.0
          var details7: String! = String(cString: [116,105,109,101,108,105,109,105,116,0], encoding: .utf8)!
          var recognizerF: Bool = true
         zhidingesv["\(recognizerF)"] = ((recognizerF ? 3 : 1) & 2)
         details7.append("\(details7.count % (Swift.max(3, 3)))")
          var verityh: String! = String(cString: [102,108,111,97,116,0], encoding: .utf8)!
          var mineO: Float = 0.0
         detaillabelf /= Swift.max(3, Float(Int(detaillabelf)))
         verityh.append("\(Int(mineO) & 1)")
         mineO /= Swift.max(Float(3), 1)
      if (4.90 * detaillabelf) > 1.22 {
          var u_viewZ: String! = String(cString: [115,105,103,110,105,110,103,0], encoding: .utf8)!
          var infok: [Any]! = [4973]
         zhidingesv[u_viewZ] = u_viewZ.count
         infok = [infok.count]
      }
         detaillabelf -= Float(Int(detaillabelf) | 2)
       var complete5: String! = String(cString: [108,105,110,101,97,114,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
         detaillabelf += Float(2 % (Swift.max(1, Int(detaillabelf))))
      }
      line0 -= zhidingesv.values.count ^ 1
      line0 %= Swift.max(1, menuL)
   repeat {
       var f_managerC: String! = String(cString: [121,95,53,57,95,112,105,99,107,108,112,102,0], encoding: .utf8)!
       var audioN: Int = 3
       var entert: Bool = true
       var eveantD: String! = String(cString: [100,105,115,109,105,115,115,97,108,0], encoding: .utf8)!
       var briefC: String! = String(cString: [109,112,101,103,118,105,100,101,111,100,97,116,97,0], encoding: .utf8)!
      repeat {
         f_managerC = "\(eveantD.count)"
         if f_managerC.count == 920143 {
            break
         }
      } while ((audioN % 1) < 5) && (f_managerC.count == 920143)
          var markf: Int = 4
          var promptt: Bool = false
          var indicesZ: [String: Any]! = [String(cString: [116,111,112,105,99,115,0], encoding: .utf8)!:String(cString: [111,110,108,105,110,101,115,0], encoding: .utf8)!]
         entert = indicesZ.keys.count == 6 && eveantD.count == 6
         markf += 3 % (Swift.max(6, markf))
         promptt = markf > 85
         indicesZ["\(markf)"] = markf
          var documenth: String! = String(cString: [105,109,112,117,108,115,101,0], encoding: .utf8)!
          var gundX: String! = String(cString: [114,101,99,111,114,100,115,0], encoding: .utf8)!
          var delete_q2: String! = String(cString: [105,110,115,116,114,117,109,101,110,116,97,116,105,111,110,0], encoding: .utf8)!
         entert = briefC.count >= 60
         documenth.append("\(documenth.count)")
         gundX = "\(gundX.count % 2)"
         delete_q2 = "\(((String(cString:[76,0], encoding: .utf8)!) == delete_q2 ? gundX.count : delete_q2.count))"
      repeat {
         f_managerC = "\(3 / (Swift.max(4, audioN)))"
         if 1255623 == f_managerC.count {
            break
         }
      } while ((audioN % (Swift.max(f_managerC.count, 3))) > 3 || 3 > (audioN % (Swift.max(2, f_managerC.count)))) && (1255623 == f_managerC.count)
         f_managerC.append("\(audioN + 2)")
         eveantD.append("\(3)")
         entert = 51 == audioN
      repeat {
         f_managerC = "\(1)"
         if 1000492 == f_managerC.count {
            break
         }
      } while (1000492 == f_managerC.count) && (entert)
         f_managerC = "\(eveantD.count)"
         audioN %= Swift.max(eveantD.count, 1)
      repeat {
         f_managerC.append("\(3)")
         if f_managerC == (String(cString:[101,114,48,97,121,99,122,117,48,54,0], encoding: .utf8)!) {
            break
         }
      } while (!f_managerC.hasSuffix("\(entert)")) && (f_managerC == (String(cString:[101,114,48,97,121,99,122,117,48,54,0], encoding: .utf8)!))
         eveantD = "\(eveantD.count)"
       var modelY: String! = String(cString: [107,101,121,115,101,116,117,112,0], encoding: .utf8)!
         entert = entert && eveantD.count >= 94
         audioN -= (eveantD.count * (entert ? 2 : 2))
      menuL -= ((entert ? 1 : 2) >> (Swift.min(briefC.count, 1)))
      if menuL == 373060 {
         break
      }
   } while (menuL == 373060) && (2 > (5 | menuL))
     var nextReusable: Float = 6414.0
     let itemsRing: Int = 6732
    var xcorrCancellable: String! = String(cString: [100,0], encoding: .utf8)!

    return xcorrCancellable

}





    
    func playerDidFinish() {

             boldTerminateNone(attributedContains:String(cString: [108,97,115,116,0], encoding: .utf8)!, safeLeft:6467)


       var engineO: String! = String(cString: [99,111,110,102,105,110,101,100,0], encoding: .utf8)!
    var sortd: Bool = true
    var homeW: String! = String(cString: [104,99,115,99,97,108,101,0], encoding: .utf8)!
      sortd = engineO.count == 95
   for _ in 0 ..< 3 {
       var prefix_d3: String! = String(cString: [117,118,108,99,0], encoding: .utf8)!
       var version8: Double = 2.0
       var styles9: Bool = false
      while (5.8 == (2.84 - version8) && 2.85 == (version8 - 2.84)) {
          var briefr: String! = String(cString: [102,114,105,101,110,100,0], encoding: .utf8)!
         version8 -= Double(2)
         briefr.append("\(briefr.count)")
         break
      }
      if version8 <= 1.42 {
         version8 *= (Double((styles9 ? 1 : 4) * Int(version8)))
      }
         version8 *= (Double((styles9 ? 1 : 3) >> (Swift.min(prefix_d3.count, 5))))
      while (version8 >= 3.49) {
         styles9 = styles9 || 84.35 >= version8
         break
      }
      while ((Double(prefix_d3.count) * version8) > 1.28) {
          var aimageh: Int = 1
          var u_width0: String! = String(cString: [102,105,110,97,108,105,122,101,114,0], encoding: .utf8)!
         version8 *= Double(aimageh)
         aimageh /= Swift.max(1, 1 << (Swift.min(1, u_width0.count)))
         u_width0 = "\(u_width0.count)"
         break
      }
      if styles9 {
          var images2: Float = 0.0
          var pathI: Double = 4.0
         version8 -= Double(3)
         images2 -= Float(Int(images2) >> (Swift.min(2, labs(2))))
         pathI += Double(Int(images2))
      }
      if !styles9 {
         version8 -= Double(2 << (Swift.min(labs(Int(version8)), 1)))
      }
          var playingg: [Any]! = [538, 197, 430]
          var contentW: String! = String(cString: [101,114,111,100,101,0], encoding: .utf8)!
         prefix_d3.append("\((prefix_d3 == (String(cString:[85,0], encoding: .utf8)!) ? prefix_d3.count : contentW.count))")
         playingg.append(playingg.count | 3)
         contentW.append("\(playingg.count ^ 2)")
      for _ in 0 ..< 2 {
         prefix_d3 = "\(1 & Int(version8))"
      }
      engineO = "\(engineO.count)"
   }

   repeat {
      sortd = (((!sortd ? homeW.count : 74) ^ homeW.count) > 74)
      if sortd ? !sortd : sortd {
         break
      }
   } while (sortd) && (sortd ? !sortd : sortd)
   while (homeW.hasPrefix("\(sortd)")) {
      sortd = !sortd
      break
   }
        
         stopPlay()
    
   for _ in 0 ..< 2 {
      engineO = "\(3 + engineO.count)"
   }
    }
}


class ZYFCommonSocket: NSObject {
var elevtIdx: Int = 0
var terminate_min: Float = 0.0
private var mainEvetLast_Array: [Any]!



    
    var generateStrExpireList: [Any]!
    var has_Request: Bool = false
    
    private let speechRecognizer = SFSpeechRecognizer(locale: Locale(identifier: "zh-CN"))!
    private var recognitionRequest: SFSpeechAudioBufferRecognitionRequest?
    private var recognitionTask: SFSpeechRecognitionTask?
    private let audioEngine = AVAudioEngine()
    private var voiceCancel: Bool = false
    private var isDetectionEnabled: Bool
    var speakTimer: DispatchSourceTimer?
    private let silenceThreshold: TimeInterval = 3.0
    private let generator = UIImpactFeedbackGenerator(style: .light)

    var resultHandler: ((String) -> Void)?
    var decibelScaleHandler: ((Float) -> Void)?

    init(isDetectionEnabled: Bool = false) {
        self.isDetectionEnabled = isDetectionEnabled
        self.generator.prepare()
    }

@discardableResult
 func prepareCheckedScheduleButtonAccelerateDeadline(layoutOther: String!) -> Float {
    var dismissP: Bool = true
    var responseB: Int = 4
      dismissP = !dismissP
   while (!dismissP) {
      responseB -= 1
      break
   }
   repeat {
       var sumM: String! = String(cString: [104,119,100,101,118,105,99,101,0], encoding: .utf8)!
         sumM.append("\(sumM.count)")
      repeat {
         sumM = "\(sumM.count)"
         if 2494156 == sumM.count {
            break
         }
      } while (2494156 == sumM.count) && (sumM.count <= 4)
      if sumM != sumM {
         sumM = "\(3)"
      }
      responseB |= sumM.count
      if responseB == 1244993 {
         break
      }
   } while (dismissP) && (responseB == 1244993)
      dismissP = !dismissP && 90 > responseB
     var iosPic: Int = 5556
     var detailslabelTerminate: Double = 9429.0
     let delegate_aRows: String! = String(cString: [100,117,97,108,0], encoding: .utf8)!
    var dialoguesPubic:Float = 0
    dialoguesPubic /= Swift.max(Float(iosPic), 1)
    dialoguesPubic += Float(detailslabelTerminate)

    return dialoguesPubic

}






    func stopRecording() {

             prepareCheckedScheduleButtonAccelerateDeadline(layoutOther:String(cString: [111,112,101,110,115,115,108,0], encoding: .utf8)!)


       var recognizedJ: Float = 5.0
    var gifG: String! = String(cString: [98,97,116,99,104,101,115,0], encoding: .utf8)!
      recognizedJ /= Swift.max(2, Float(Int(recognizedJ) / (Swift.max(gifG.count, 10))))

       var speech7: [String: Any]! = [String(cString: [115,98,117,118,0], encoding: .utf8)!:643, String(cString: [115,116,114,105,112,112,101,100,0], encoding: .utf8)!:620]
      for _ in 0 ..< 1 {
          var aymentP: String! = String(cString: [114,101,112,114,101,115,101,110,116,97,98,108,101,0], encoding: .utf8)!
          var type_e03: [String: Any]! = [String(cString: [112,109,107,0], encoding: .utf8)!:true]
          var next0: Double = 3.0
          var record8: [Any]! = [530, 505, 274]
          var navigation7: Float = 0.0
         speech7["\(next0)"] = record8.count * Int(next0)
         aymentP = "\((aymentP == (String(cString:[89,0], encoding: .utf8)!) ? type_e03.values.count : aymentP.count))"
         type_e03 = ["\(type_e03.values.count)": aymentP.count / 3]
         record8.append(Int(navigation7))
         navigation7 /= Swift.max(Float(Int(navigation7) << (Swift.min(labs(3), 4))), 4)
      }
      repeat {
         speech7 = ["\(speech7.values.count)": speech7.values.count]
         if speech7.count == 1216597 {
            break
         }
      } while (speech7.count == 1216597) && (1 == (speech7.count + speech7.count) || 5 == (1 + speech7.count))
      repeat {
         speech7["\(speech7.count)"] = speech7.count
         if 2673639 == speech7.count {
            break
         }
      } while (2673639 == speech7.count) && (2 >= speech7.count)
      gifG.append("\((gifG == (String(cString:[56,0], encoding: .utf8)!) ? gifG.count : speech7.values.count))")
        
        generator.impactOccurred()
        
        DispatchQueue.main.asyncAfter(deadline: .now() + 0.1) { [weak self] in
            guard let self = self else { return }

   repeat {
      gifG.append("\(1 + gifG.count)")
      if (String(cString:[100,54,55,120,99,106,97,115,110,121,0], encoding: .utf8)!) == gifG {
         break
      }
   } while ((gifG.count % 2) >= 5) && ((String(cString:[100,54,55,120,99,106,97,115,110,121,0], encoding: .utf8)!) == gifG)
            self.snapScopeImage()
   if 5 > (3 - gifG.count) {
      recognizedJ *= Float(Int(recognizedJ))
   }
            self.audioEngine.stop()
            self.recognitionRequest?.endAudio()
            let register_z = AVAudioSession.sharedInstance()
            try! register_z.setCategory(.playback, mode: .default)
            try! register_z.setActive(true, options: .notifyOthersOnDeactivation)
        }
        
    }

@discardableResult
 func bestHotBusLoad() -> [Any]! {
    var headZ: [Any]! = [317, 940]
    var liholderlabelW: Double = 1.0
       var w_imageL: [String: Any]! = [String(cString: [98,97,100,103,101,0], encoding: .utf8)!:[String(cString: [114,101,97,99,104,97,98,105,108,105,116,121,0], encoding: .utf8)!:506, String(cString: [108,97,118,102,117,116,105,108,115,0], encoding: .utf8)!:914]]
       var collF: Bool = false
      if 1 <= (1 + w_imageL.keys.count) {
         collF = !collF
      }
      for _ in 0 ..< 1 {
         collF = !collF
      }
         w_imageL = ["\(w_imageL.values.count)": w_imageL.values.count >> (Swift.min(labs(2), 4))]
      repeat {
         w_imageL = ["\(w_imageL.keys.count)": (1 & (collF ? 4 : 2))]
         if w_imageL.count == 718720 {
            break
         }
      } while (w_imageL.count == 718720) && (collF)
      for _ in 0 ..< 3 {
         w_imageL["\(collF)"] = 1
      }
      for _ in 0 ..< 2 {
         w_imageL["\(collF)"] = 2
      }
      liholderlabelW += Double(2 - w_imageL.values.count)
    var unselectedy: String! = String(cString: [114,101,112,114,101,115,101,110,116,105,110,103,0], encoding: .utf8)!
      liholderlabelW /= Swift.max(3, Double(headZ.count | unselectedy.count))
   return headZ

}






    func cancelRecording() {

             bestHotBusLoad()


       var btno: Bool = false
    var valuelabell: String! = String(cString: [109,117,108,116,105,99,97,115,116,0], encoding: .utf8)!
    var namelabelV: [Any]! = [761, 652, 523]
       var weak_fnu: [Any]! = [String(cString: [99,101,108,116,0], encoding: .utf8)!, String(cString: [111,110,99,101,0], encoding: .utf8)!, String(cString: [110,117,108,108,115,114,99,0], encoding: .utf8)!]
       var convertV: String! = String(cString: [99,117,114,116,97,105,110,115,0], encoding: .utf8)!
      if 1 <= (3 * weak_fnu.count) {
         convertV = "\(2 - convertV.count)"
      }
         weak_fnu.append(3)
         convertV.append("\(weak_fnu.count ^ 3)")
       var detectl: Int = 5
       var aymentQ: Int = 1
          var type_v0M: Int = 0
         detectl *= 2
         type_v0M += 1
      repeat {
         aymentQ |= detectl
         if 1955520 == aymentQ {
            break
         }
      } while (1955520 == aymentQ) && ((1 >> (Swift.min(3, labs(detectl)))) == 3)
      namelabelV.append(namelabelV.count | valuelabell.count)
   for _ in 0 ..< 3 {
       var launchz: Float = 5.0
       var engineg: Double = 0.0
         launchz -= Float(2 | Int(engineg))
      if (Double(launchz) * engineg) <= 5.29 || (Double(launchz) * engineg) <= 5.29 {
          var r_view9: Double = 1.0
         launchz /= Swift.max(3, Float(Int(engineg) >> (Swift.min(labs(Int(r_view9)), 1))))
      }
          var replaceS: String! = String(cString: [98,117,105,108,100,115,0], encoding: .utf8)!
          var likeX: Float = 4.0
          var thresholdb: [Any]! = [693, 237, 850]
         launchz -= Float(replaceS.count)
         replaceS.append("\(Int(likeX))")
         likeX *= Float(Int(likeX) & thresholdb.count)
         thresholdb = [Int(likeX)]
         engineg += Double(Int(launchz) >> (Swift.min(1, labs(3))))
      for _ in 0 ..< 1 {
         launchz /= Swift.max(Float(2), 3)
      }
         launchz *= Float(Int(engineg) - Int(launchz))
      btno = (String(cString:[57,0], encoding: .utf8)!) == valuelabell
   }

   repeat {
      namelabelV = [namelabelV.count]
      if namelabelV.count == 1733289 {
         break
      }
   } while (5 <= (namelabelV.count + 3)) && (namelabelV.count == 1733289)
       var instanceD: String! = String(cString: [112,114,101,112,97,114,101,100,0], encoding: .utf8)!
       var audioT: String! = String(cString: [115,105,110,113,102,0], encoding: .utf8)!
       var utilsf: String! = String(cString: [115,116,114,101,115,115,0], encoding: .utf8)!
       var sections9: Float = 3.0
       var currentX: Float = 4.0
      repeat {
         utilsf.append("\(utilsf.count >> (Swift.min(audioT.count, 3)))")
         if utilsf.count == 3709716 {
            break
         }
      } while (4.72 == (1.74 * sections9) && 3.29 == (1.74 * sections9)) && (utilsf.count == 3709716)
         instanceD = "\(1 | instanceD.count)"
      repeat {
          var recordsA: Float = 5.0
          var tapO: String! = String(cString: [105,110,116,101,114,110,101,100,0], encoding: .utf8)!
          var layoutJ: Float = 4.0
          var bufferB: String! = String(cString: [109,111,110,107,101,121,115,97,117,100,105,111,0], encoding: .utf8)!
         instanceD = "\(Int(recordsA))"
         recordsA /= Swift.max(Float(tapO.count), 4)
         tapO = "\(tapO.count)"
         layoutJ -= Float(3 % (Swift.max(10, tapO.count)))
         bufferB = "\((bufferB == (String(cString:[122,0], encoding: .utf8)!) ? bufferB.count : Int(layoutJ)))"
         if instanceD == (String(cString:[110,103,50,101,103,0], encoding: .utf8)!) {
            break
         }
      } while (instanceD == (String(cString:[110,103,50,101,103,0], encoding: .utf8)!)) && (instanceD == String(cString:[67,0], encoding: .utf8)! || audioT.count < 5)
      if 3 >= instanceD.count || 3 >= audioT.count {
         audioT.append("\(Int(sections9) % 2)")
      }
      if (audioT.count - Int(currentX)) > 2 && 2 > (audioT.count - Int(currentX)) {
         currentX *= Float(utilsf.count)
      }
          var responseC: String! = String(cString: [116,105,109,115,116,97,109,112,0], encoding: .utf8)!
          var bottomR: Int = 5
         sections9 *= Float(bottomR << (Swift.min(5, labs(3))))
         responseC.append("\(2 << (Swift.min(2, responseC.count)))")
         bottomR %= Swift.max(4, (responseC == (String(cString:[87,0], encoding: .utf8)!) ? responseC.count : responseC.count))
      for _ in 0 ..< 1 {
         utilsf.append("\((instanceD == (String(cString:[83,0], encoding: .utf8)!) ? instanceD.count : Int(currentX)))")
      }
          var contextw: Bool = false
          var delegate_3A: Int = 3
         instanceD.append("\(((contextw ? 2 : 5) + Int(currentX)))")
         contextw = delegate_3A <= 68
         delegate_3A ^= 2
      namelabelV = [((btno ? 5 : 4))]
        
        voiceCancel = true
   repeat {
      valuelabell = "\((valuelabell.count + (btno ? 4 : 4)))"
      if 3210230 == valuelabell.count {
         break
      }
   } while (namelabelV.count < valuelabell.count) && (3210230 == valuelabell.count)
      namelabelV = [((String(cString:[74,0], encoding: .utf8)!) == valuelabell ? valuelabell.count : namelabelV.count)]
        stopRecording()
    }


    
    func startRecording() {
        
        generator.impactOccurred()
        audioEngine.stop()
        audioEngine.reset()

        DispatchQueue.main.asyncAfter(deadline: .now() + 0.1) { [weak self] in
            
            guard let self = self else { return }

            checkAliToken()
            
            if let recognitionTask = self.recognitionTask {
                recognitionTask.cancel()
                self.recognitionTask = nil
            }

            self.voiceCancel = false
            
            let register_z = AVAudioSession.sharedInstance()
            try! register_z.setCategory(.record, mode: .measurement, options: .duckOthers)
            try! register_z.setActive(true, options: .notifyOthersOnDeactivation)

            self.recognitionRequest = SFSpeechAudioBufferRecognitionRequest()

            let window_61 = self.audioEngine.inputNode

            guard let recognitionRequest = self.recognitionRequest else { fatalError("Unable to create a SFSpeechAudioBufferRecognitionRequest object") }

            recognitionRequest.shouldReportPartialResults = true
            
            self.recognitionTask = self.speechRecognizer.recognitionTask(with: recognitionRequest) { result, error in
                var nextButton = false
                
                if let result = result {
                    
                    if self.isDetectionEnabled {
                        self.snapScopeImage()
                        self.fatalFillDateLengthBundleArrow()
                    }
                    print("——————说话中: \(result.bestTranscription.formattedString)")
                    nextButton = result.isFinal
                    NotificationCenter.default.post(name: NSNotification.Name("VoiceMessageNotificationName"), object: result.bestTranscription.formattedString)
                }

                if error != nil || nextButton {
                    self.audioEngine.stop()
                    window_61.removeTap(onBus: 0)

                    self.recognitionRequest = nil
                    self.recognitionTask = nil

                    if let result = result {
                        if self.voiceCancel == false {
                            self.resultHandler?(result.bestTranscription.formattedString)
                        }
                    }
                }
            }

            let data3 = window_61.outputFormat(forBus: 0)
            window_61.installTap(onBus: 0, bufferSize: 1024, format: data3) { (buffer: AVAudioPCMBuffer, when: AVAudioTime) in
                self.recognitionRequest?.append(buffer)
                
                
                guard let channelData = buffer.floatChannelData else { return }
                
                
                let serviceView = vDSP_Length(buffer.frameLength)
                var volume: Float = 0
                vDSP_rmsqv(channelData[0], 1, &volume, serviceView)
                
                
                let header = 20 * log10(volume)
                
                
                DispatchQueue.main.async {
                    
                    let records = 1.0 + CGFloat((header + 50) / 50.0)
                    let gesture = max(1.0, min(records, 1.5))
                    
                    if gesture > 1.0 {
                        self.decibelScaleHandler?(Float(gesture))
                    }
                }
            }
            
            self.audioEngine.prepare()
            try! self.audioEngine.start()
        }
        
    }

@discardableResult
 func chatSumNowStatusAlertInclude() -> Double {
    var qlabelo: String! = String(cString: [97,114,99,0], encoding: .utf8)!
    var emptyj: String! = String(cString: [114,101,102,105,108,108,0], encoding: .utf8)!
      emptyj = "\(((String(cString:[75,0], encoding: .utf8)!) == emptyj ? qlabelo.count : emptyj.count))"
      emptyj = "\(1)"
   if qlabelo == String(cString:[114,0], encoding: .utf8)! {
      emptyj.append("\(emptyj.count)")
   }
      qlabelo = "\(2 >> (Swift.min(5, qlabelo.count)))"
     let fontObj: Bool = false
     var documentCenter: String! = String(cString: [116,97,108,108,0], encoding: .utf8)!
     let sepakBase: Int = 732
    var libvorbisAlanguage:Double = 0
    libvorbisAlanguage += Double(fontObj ? 52 : 45)
    libvorbisAlanguage += Double(sepakBase)

    return libvorbisAlanguage

}






    private func fatalFillDateLengthBundleArrow() {

             chatSumNowStatusAlertInclude()


       var areaz: [Any]! = [325, 708]
    var parametersS: Double = 5.0
    var answerl: Int = 5
      areaz.append(Int(parametersS) + answerl)

   repeat {
      areaz = [areaz.count & 3]
      if 2312029 == areaz.count {
         break
      }
   } while (2312029 == areaz.count) && (4 == areaz.count)

      areaz = [2]
        if speakTimer == nil {
            speakTimer?.cancel()
       var observationsS: String! = String(cString: [111,117,116,112,117,116,115,0], encoding: .utf8)!
         observationsS.append("\(2)")
       var keywordsP: String! = String(cString: [105,110,116,101,114,120,121,0], encoding: .utf8)!
          var shouk: String! = String(cString: [120,109,117,108,0], encoding: .utf8)!
          var url8: Int = 4
          var alifastv: Int = 3
         observationsS = "\(2 ^ alifastv)"
         shouk.append("\(url8 * 3)")
         url8 ^= 1
         alifastv /= Swift.max(3, ((String(cString:[71,0], encoding: .utf8)!) == shouk ? shouk.count : url8))
      areaz = [2 >> (Swift.min(5, areaz.count))]
            speakTimer = DispatchSource.makeTimerSource(queue: DispatchQueue.main)
      parametersS -= Double(answerl >> (Swift.min(labs(Int(parametersS)), 4)))
            speakTimer?.schedule(deadline: .now() + silenceThreshold, repeating: .never)
            speakTimer?.setEventHandler { [weak self] in
       var int_at6: Double = 5.0
          var common6: Bool = true
          var flowF: String! = String(cString: [112,114,111,116,111,0], encoding: .utf8)!
          var bundler: String! = String(cString: [119,97,118,101,108,101,116,0], encoding: .utf8)!
         int_at6 /= Swift.max(Double(flowF.count), 1)
         common6 = !common6 || bundler.count > 49
         flowF.append("\(((common6 ? 5 : 3) / (Swift.max(bundler.count, 7))))")
         int_at6 += Double(1 & Int(int_at6))
      for _ in 0 ..< 1 {
         int_at6 -= Double(2 * Int(int_at6))
      }
      answerl /= Swift.max(4, answerl)
                self?.stopRecording()
            }
            speakTimer?.resume()
        }
    }

@discardableResult
 func processQuestionPopMenuLineSum(textEveant: [String: Any]!, promptVipicon: Float) -> Bool {
    var arrayG: String! = String(cString: [102,97,99,101,0], encoding: .utf8)!
    var pathl: String! = String(cString: [109,111,100,105,102,105,101,100,0], encoding: .utf8)!
      pathl.append("\(1)")
   for _ in 0 ..< 2 {
      arrayG = "\(arrayG.count)"
   }
   repeat {
      pathl = "\(1 << (Swift.min(4, arrayG.count)))"
      if pathl.count == 1267116 {
         break
      }
   } while (pathl.count == 1267116) && (arrayG != pathl)
      arrayG.append("\(arrayG.count)")
     var showDatas: Float = 6640.0
     var levelStatues: Bool = false
    var  completionsConsumeBanding:Bool = false
    completionsConsumeBanding = showDatas > 29
    levelStatues = true
    completionsConsumeBanding = levelStatues

    return completionsConsumeBanding

}






    
    private func snapScopeImage() {

             processQuestionPopMenuLineSum(textEveant:[String(cString: [109,105,110,105,109,97,108,108,121,0], encoding: .utf8)!:598, String(cString: [101,112,108,121,0], encoding: .utf8)!:177], promptVipicon:5118.0)


       var commentK: Double = 4.0
    var requestr: String! = String(cString: [97,99,102,102,0], encoding: .utf8)!
      requestr.append("\(Int(commentK))")

      requestr = "\(Int(commentK) - 3)"

   if requestr.hasSuffix("\(commentK)") {
      requestr.append("\(2)")
   }
        speakTimer?.cancel()
        speakTimer = nil
    }
    
}
