
import Foundation

import UIKit

class UShouGundongCell: UICollectionViewCell {
var numConfig_string: String!
private var commentSpace: Double = 0.0
var screenSize: Float = 0.0




    @IBOutlet weak var sepakImage: UIImageView!

@discardableResult
 func failureScopeDownloadImpactOwnerCacheView(reusableLike: [Any]!) -> UIView! {
    var executeX: String! = String(cString: [111,110,116,111,0], encoding: .utf8)!
    var call2: Bool = false
   repeat {
      executeX = "\(1 << (Swift.min(1, executeX.count)))"
      if executeX.count == 1986734 {
         break
      }
   } while (5 == executeX.count) && (executeX.count == 1986734)
   repeat {
      call2 = (17 <= (executeX.count & (!call2 ? 17 : executeX.count)))
      if call2 ? !call2 : call2 {
         break
      }
   } while (!call2) && (call2 ? !call2 : call2)
   repeat {
      call2 = executeX.count >= 8
      if call2 ? !call2 : call2 {
         break
      }
   } while (1 == executeX.count) && (call2 ? !call2 : call2)
     let speechCandidate: [Any]! = [89, 56]
     var typelabelFast: Int = 5656
     let urlsStop: String! = String(cString: [119,114,106,112,103,99,111,109,0], encoding: .utf8)!
    var impFillin = UIView(frame:CGRect.zero)
    impFillin.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    impFillin.alpha = 0.4
    impFillin.frame = CGRect(x: 23, y: 256, width: 0, height: 0)

    
    var impFillinFrame = impFillin.frame
    impFillinFrame.size = CGSize(width: 240, height: 296)
    impFillin.frame = impFillinFrame
    if impFillin.alpha > 0.0 {
         impFillin.alpha = 0.0
    }
    if impFillin.isHidden {
         impFillin.isHidden = false
    }
    if !impFillin.isUserInteractionEnabled {
         impFillin.isUserInteractionEnabled = true
    }

    return impFillin

}





    override func awakeFromNib() {

         let nputSpecifier: UIView! = failureScopeDownloadImpactOwnerCacheView(reusableLike:[String(cString: [97,102,116,101,114,108,105,102,101,0], encoding: .utf8)!, String(cString: [99,105,112,104,101,114,0], encoding: .utf8)!, String(cString: [120,95,55,56,95,109,112,100,98,0], encoding: .utf8)!])

      if nputSpecifier != nil {
          let nputSpecifier_tag = nputSpecifier.tag
          self.addSubview(nputSpecifier)
      }
      else {
          print("nputSpecifier is nil")      }



       var valuelabelz: Double = 5.0
    var menuk: String! = String(cString: [117,110,116,114,97,99,107,0], encoding: .utf8)!
       var popup4: Int = 4
       var layout9: Double = 5.0
      while (popup4 > 5) {
         popup4 += popup4 >> (Swift.min(3, labs(2)))
         break
      }
      for _ in 0 ..< 3 {
          var bottomJ: String! = String(cString: [119,101,98,112,97,103,101,115,0], encoding: .utf8)!
          var change4: String! = String(cString: [115,117,112,112,111,114,116,105,110,103,0], encoding: .utf8)!
          var nicknamew: String! = String(cString: [97,112,112,101,97,114,105,110,103,0], encoding: .utf8)!
         popup4 *= 2
         bottomJ.append("\(nicknamew.count)")
         change4 = "\(change4.count * 2)"
         nicknamew = "\(nicknamew.count / (Swift.max(8, bottomJ.count)))"
      }
         layout9 += Double(1)
      if 4.21 >= layout9 {
          var aimageb: [String: Any]! = [String(cString: [104,101,120,99,104,97,114,105,110,116,0], encoding: .utf8)!:596, String(cString: [116,114,97,105,110,0], encoding: .utf8)!:880]
         layout9 /= Swift.max(1, Double(aimageb.keys.count))
      }
      repeat {
          var indicesZ: String! = String(cString: [109,98,108,111,111,112,0], encoding: .utf8)!
          var sendz: String! = String(cString: [121,117,118,110,118,99,0], encoding: .utf8)!
          var resultw: String! = String(cString: [98,114,105,100,103,101,100,0], encoding: .utf8)!
          var size__cP: Int = 1
         layout9 *= Double(sendz.count << (Swift.min(2, labs(Int(layout9)))))
         indicesZ = "\(2)"
         sendz = "\(indicesZ.count - resultw.count)"
         resultw = "\(3 + resultw.count)"
         size__cP >>= Swift.min(2, indicesZ.count)
         if 1244677.0 == layout9 {
            break
         }
      } while (5 == (popup4 * 2)) && (1244677.0 == layout9)
          var class_09: [Any]! = [252, 392, 486]
          var brieft: [String: Any]! = [String(cString: [119,101,108,115,101,110,99,100,101,109,111,0], encoding: .utf8)!:String(cString: [100,101,102,105,110,101,115,0], encoding: .utf8)!, String(cString: [101,120,112,108,105,99,105,116,108,121,0], encoding: .utf8)!:String(cString: [101,116,104,0], encoding: .utf8)!]
         popup4 <<= Swift.min(brieft.values.count, 3)
         class_09.append(class_09.count)
         brieft["\(class_09.count)"] = class_09.count | class_09.count
      valuelabelz += Double(1)

   if valuelabelz > Double(menuk.count) {
      menuk.append("\(Int(valuelabelz))")
   }
        super.awakeFromNib()
   while (1 > (menuk.count - Int(valuelabelz))) {
      valuelabelz /= Swift.max(2, Double(menuk.count))
      break
   }
        
        sepakImage.layer.borderWidth = 3
   while (menuk.count < Int(valuelabelz)) {
       var briefx: Bool = false
       var records5: String! = String(cString: [103,111,108,100,0], encoding: .utf8)!
       var relationP: [Any]! = [332, 161, 11]
       var channelo: Float = 0.0
          var alabelC: String! = String(cString: [114,101,118,97,108,105,100,97,116,101,100,0], encoding: .utf8)!
         channelo /= Swift.max(5, Float(records5.count * 3))
         alabelC.append("\(alabelC.count)")
          var descripti: [String: Any]! = [String(cString: [112,104,121,115,0], encoding: .utf8)!:598, String(cString: [98,105,119,103,116,0], encoding: .utf8)!:824]
         briefx = descripti.values.count <= 39
         relationP = [Int(channelo)]
       var home4: String! = String(cString: [119,97,105,116,105,110,103,0], encoding: .utf8)!
          var valuei: Double = 2.0
          var stop_: String! = String(cString: [116,111,107,101,104,0], encoding: .utf8)!
         channelo -= Float(stop_.count >> (Swift.min(records5.count, 3)))
         valuei /= Swift.max(2, Double(Int(valuei) / 3))
         stop_.append("\(Int(valuei))")
          var actionK: [String: Any]! = [String(cString: [103,105,116,104,117,98,0], encoding: .utf8)!:[4173.0]]
         records5 = "\(3)"
         actionK = ["\(actionK.count)": 1]
       var recordsv: Double = 0.0
       var constraintJ: Double = 3.0
          var zhidinges5: String! = String(cString: [111,100,109,108,0], encoding: .utf8)!
          var about6: [Any]! = [371, 706, 186]
         recordsv /= Swift.max(Double(2 * zhidinges5.count), 3)
         zhidinges5.append("\(2 << (Swift.min(1, about6.count)))")
         about6.append(3)
         recordsv /= Swift.max(Double(1), 1)
          var resourcesV: Bool = false
          var valueJ: String! = String(cString: [101,103,111,108,111,109,98,0], encoding: .utf8)!
         relationP.append(records5.count + 1)
         resourcesV = (5 >= ((resourcesV ? 5 : valueJ.count) & valueJ.count))
         home4.append("\((Int(recordsv) % (Swift.max(6, (briefx ? 1 : 3)))))")
      while (briefx) {
         records5 = "\(relationP.count)"
         break
      }
      menuk.append("\(records5.count % 2)")
      break
   }
        sepakImage.layer.cornerRadius = 16
        sepakImage.layer.masksToBounds = true
    }

}
