
import Foundation

import UIKit

class CMCommonHandingCell: UITableViewCell {
private var length_padding: Double = 0.0
private var ypricelabelStartName_str: String!




    @IBOutlet weak var meQlabel: UILabel!
    @IBOutlet weak var avator: UIImageView!

@discardableResult
 func numberMainHistoryCommitReceive(buttonBtn: String!, likeVersion: Float, itemsAyment: String!) -> String! {
    var attributesE: [Any]! = [315, 74]
    var listy: [Any]! = [[String(cString: [116,114,101,97,100,0], encoding: .utf8)!:[244, 154]]]
   while (listy.count <= attributesE.count) {
      attributesE = [2]
      break
   }
       var line0: Int = 1
       var remark2: Float = 4.0
         line0 *= Int(remark2)
       var numberc: String! = String(cString: [104,105,103,104,108,105,103,116,104,101,100,0], encoding: .utf8)!
      repeat {
         remark2 -= (Float((String(cString:[112,0], encoding: .utf8)!) == numberc ? Int(remark2) : numberc.count))
         if 1647092.0 == remark2 {
            break
         }
      } while (remark2 <= 3.92) && (1647092.0 == remark2)
          var navigationG: String! = String(cString: [97,99,99,101,115,115,105,98,105,108,105,116,121,0], encoding: .utf8)!
          var reusabler: String! = String(cString: [109,111,100,112,108,117,103,0], encoding: .utf8)!
         line0 %= Swift.max(1, Int(remark2))
         navigationG.append("\((navigationG == (String(cString:[109,0], encoding: .utf8)!) ? reusabler.count : navigationG.count))")
         reusabler = "\(reusabler.count ^ 1)"
      if 1.96 <= (2.19 + remark2) || (remark2 + Float(numberc.count)) <= 2.19 {
          var recognizedJ: [Any]! = [559, 430, 301]
          var commentd: String! = String(cString: [109,97,116,114,105,120,0], encoding: .utf8)!
         numberc = "\(((String(cString:[54,0], encoding: .utf8)!) == commentd ? commentd.count : numberc.count))"
         recognizedJ.append(1 << (Swift.min(2, recognizedJ.count)))
      }
      repeat {
         line0 ^= Int(remark2) + 3
         if line0 == 812074 {
            break
         }
      } while ((remark2 * 2.33) > 4.49 && (remark2 * 2.33) > 1.38) && (line0 == 812074)
      listy = [3]
   for _ in 0 ..< 1 {
       var avatarg: Double = 4.0
       var historyq: String! = String(cString: [105,110,116,101,114,97,116,105,111,110,0], encoding: .utf8)!
       var firstr: [Any]! = [640, 568, 467]
       var action4: Double = 4.0
       var c_viewb: Bool = true
      repeat {
         firstr = [firstr.count >> (Swift.min(labs(2), 1))]
         if 3550103 == firstr.count {
            break
         }
      } while (3550103 == firstr.count) && ((firstr.count & historyq.count) >= 3 && (historyq.count & firstr.count) >= 3)
      if 1 > historyq.count || !c_viewb {
         historyq = "\(((String(cString:[70,0], encoding: .utf8)!) == historyq ? (c_viewb ? 2 : 4) : historyq.count))"
      }
          var fastx: String! = String(cString: [111,112,116,105,109,105,122,97,116,105,111,110,0], encoding: .utf8)!
          var lines: String! = String(cString: [108,101,103,97,99,121,0], encoding: .utf8)!
          var pictureW: String! = String(cString: [101,120,112,114,108,105,115,116,0], encoding: .utf8)!
         firstr = [Int(avatarg) + firstr.count]
         fastx = "\(pictureW.count)"
         lines = "\(3 + lines.count)"
         pictureW.append("\(pictureW.count - 2)")
       var flowP: [String: Any]! = [String(cString: [113,117,97,114,116,0], encoding: .utf8)!:109, String(cString: [115,112,97,114,115,101,0], encoding: .utf8)!:867, String(cString: [115,104,111,114,116,101,110,0], encoding: .utf8)!:576]
          var alifaste: String! = String(cString: [112,97,114,101,110,116,104,101,115,105,115,0], encoding: .utf8)!
         firstr.append(flowP.values.count)
         alifaste = "\(alifaste.count / (Swift.max(2, 4)))"
         action4 -= Double(historyq.count)
         avatarg -= Double(1 ^ Int(avatarg))
      while (c_viewb) {
          var readC: String! = String(cString: [120,95,52,52,95,99,104,101,99,107,109,97,114,107,0], encoding: .utf8)!
          var relationX: String! = String(cString: [114,101,102,101,114,101,110,101,0], encoding: .utf8)!
          var o_animationJ: Bool = true
         flowP = ["\(firstr.count)": 2]
         readC.append("\(2 << (Swift.min(2, readC.count)))")
         relationX.append("\(relationX.count)")
         break
      }
      while (1.89 == (2.86 * avatarg) && (1 + firstr.count) == 1) {
          var main_pm: String! = String(cString: [112,97,105,114,115,0], encoding: .utf8)!
          var history4: [Any]! = [9334]
          var speedsN: String! = String(cString: [100,105,115,99,117,115,115,0], encoding: .utf8)!
         firstr.append(Int(action4))
         main_pm = "\(3)"
         history4.append(speedsN.count >> (Swift.min(labs(1), 5)))
         speedsN = "\(history4.count)"
         break
      }
      if flowP.values.contains { $0 as? Double == avatarg } {
          var candidatev: Double = 4.0
          var findp: [Any]! = [233, 969]
          var time_ed: String! = String(cString: [118,97,114,105,97,110,116,0], encoding: .utf8)!
          var promptb: Float = 3.0
          var chata: Float = 1.0
         flowP = ["\(findp.count)": 2]
         candidatev += Double(1 >> (Swift.min(labs(Int(candidatev)), 3)))
         findp = [3 >> (Swift.min(labs(Int(promptb)), 3))]
         time_ed.append("\(Int(promptb) >> (Swift.min(labs(3), 1)))")
         chata -= Float(Int(promptb))
      }
      while (!c_viewb) {
          var dicB: Int = 3
          var namelabelG: String! = String(cString: [97,117,116,104,101,110,116,105,99,97,116,105,111,110,0], encoding: .utf8)!
         action4 *= (Double(flowP.keys.count >> (Swift.min(5, labs((c_viewb ? 3 : 1))))))
         dicB |= namelabelG.count << (Swift.min(4, labs(dicB)))
         namelabelG.append("\(dicB % (Swift.max(namelabelG.count, 2)))")
         break
      }
      for _ in 0 ..< 2 {
         firstr = [1 & Int(avatarg)]
      }
      if 3 >= firstr.count {
          var k_countY: Double = 4.0
          var loginP: Double = 5.0
         firstr = [(historyq == (String(cString:[117,0], encoding: .utf8)!) ? Int(action4) : historyq.count)]
         k_countY += Double(Int(loginP))
         loginP -= Double(Int(k_countY) / 1)
      }
      for _ in 0 ..< 1 {
         historyq.append("\((historyq.count << (Swift.min(5, labs((c_viewb ? 1 : 5))))))")
      }
         avatarg *= (Double(3 + (c_viewb ? 1 : 3)))
      listy = [listy.count]
   }
       var picker5: Double = 4.0
       var isbdingF: [Any]! = [599, 779]
       var backL: String! = String(cString: [102,97,99,105,108,105,116,97,116,101,0], encoding: .utf8)!
      while ((isbdingF.count / (Swift.max(backL.count, 6))) <= 1 && (isbdingF.count / (Swift.max(backL.count, 10))) <= 1) {
         backL.append("\(Int(picker5))")
         break
      }
       var handlerq: String! = String(cString: [99,111,99,111,97,0], encoding: .utf8)!
       var create6: String! = String(cString: [100,117,114,98,0], encoding: .utf8)!
          var answerm: Double = 1.0
          var completionp: String! = String(cString: [116,121,112,101,100,101,102,0], encoding: .utf8)!
         isbdingF = [create6.count - 3]
         answerm -= Double(completionp.count % (Swift.max(6, Int(answerm))))
         completionp.append("\((completionp == (String(cString:[89,0], encoding: .utf8)!) ? Int(answerm) : completionp.count))")
          var buttonz: [Any]! = [true]
          var recordp: String! = String(cString: [108,101,115,115,0], encoding: .utf8)!
         handlerq = "\(isbdingF.count)"
         buttonz = [recordp.count + 1]
         recordp.append("\(1)")
      if (picker5 / (Swift.max(Double(isbdingF.count), 7))) <= 2.99 {
         picker5 /= Swift.max(Double(isbdingF.count), 2)
      }
         create6.append("\((backL == (String(cString:[98,0], encoding: .utf8)!) ? backL.count : Int(picker5)))")
      while (handlerq.hasSuffix("\(create6.count)")) {
         handlerq.append("\(isbdingF.count)")
         break
      }
      repeat {
         create6.append("\(Int(picker5))")
         if create6 == (String(cString:[55,105,111,101,109,55,0], encoding: .utf8)!) {
            break
         }
      } while (create6 == (String(cString:[55,105,111,101,109,55,0], encoding: .utf8)!)) && (1 <= backL.count || create6 == String(cString:[111,0], encoding: .utf8)!)
         handlerq = "\(create6.count)"
      listy = [3]
     var constraintRemove: String! = String(cString: [101,120,112,101,99,116,115,0], encoding: .utf8)!
     var self_dVersion: String! = String(cString: [98,95,49,50,0], encoding: .utf8)!
     let showKeywords: Double = 7398.0
    var argxiRounded: String! = String(cString: [98,0], encoding: .utf8)!
    argxiRounded += constraintRemove
    argxiRounded += self_dVersion

    return argxiRounded

}






    override func setSelected(_ selected: Bool, animated: Bool) {

         let meshCnt: String! = numberMainHistoryCommitReceive(buttonBtn:String(cString: [102,102,109,97,108,0], encoding: .utf8)!, likeVersion:4666.0, itemsAyment:String(cString: [102,100,99,116,100,115,112,0], encoding: .utf8)!)

      print(meshCnt)
      let meshCnt_len = meshCnt?.count ?? 0



       var beginT: Int = 1
    var socketK: String! = String(cString: [104,111,108,100,105,110,103,0], encoding: .utf8)!
    var displayi: Double = 1.0
   repeat {
       var modele: [Any]! = [[[417, 763, 13]]]
       var controllerK: Double = 2.0
      for _ in 0 ..< 1 {
          var gundh: String! = String(cString: [109,118,99,111,117,110,116,0], encoding: .utf8)!
         modele.append(modele.count)
         gundh.append("\(gundh.count)")
      }
         controllerK -= Double(modele.count)
      repeat {
         controllerK += Double(Int(controllerK) << (Swift.min(4, labs(2))))
         if 3137232.0 == controllerK {
            break
         }
      } while (3137232.0 == controllerK) && ((1 / (Swift.max(7, modele.count))) >= 3 || 4 >= (1 / (Swift.max(10, modele.count))))
         modele.append(modele.count)
          var shouL: Float = 1.0
         modele = [Int(shouL) - Int(controllerK)]
      for _ in 0 ..< 3 {
          var avatorn: Bool = false
          var mineO: String! = String(cString: [97,100,100,114,105,110,102,111,0], encoding: .utf8)!
         controllerK /= Swift.max(1, Double(modele.count))
         avatorn = mineO == (String(cString:[54,0], encoding: .utf8)!)
         mineO.append("\(mineO.count)")
      }
      socketK = "\(beginT << (Swift.min(labs(Int(controllerK)), 1)))"
      if socketK == (String(cString:[52,115,95,95,122,56,56,0], encoding: .utf8)!) {
         break
      }
   } while (beginT <= socketK.count) && (socketK == (String(cString:[52,115,95,95,122,56,56,0], encoding: .utf8)!))
   for _ in 0 ..< 3 {
       var versionK: String! = String(cString: [105,113,109,102,0], encoding: .utf8)!
       var fileH: Int = 4
       var table_: Double = 4.0
      repeat {
         versionK.append("\(3 / (Swift.max(fileH, 8)))")
         if (String(cString:[54,111,114,115,0], encoding: .utf8)!) == versionK {
            break
         }
      } while ((String(cString:[54,111,114,115,0], encoding: .utf8)!) == versionK) && (1 <= (4 + versionK.count))
         fileH |= ((String(cString:[65,0], encoding: .utf8)!) == versionK ? versionK.count : fileH)
      if 5 < fileH {
         versionK.append("\(Int(table_) * fileH)")
      }
         versionK.append("\(1)")
          var progressY: String! = String(cString: [99,97,116,99,104,105,110,103,0], encoding: .utf8)!
          var safev: Double = 0.0
         versionK = "\(versionK.count << (Swift.min(5, labs(Int(table_)))))"
         progressY = "\(3)"
         safev /= Swift.max(1, Double(3))
          var graphicsD: String! = String(cString: [99,111,109,112,97,116,0], encoding: .utf8)!
         table_ *= Double(fileH * 3)
         graphicsD.append("\(1)")
      repeat {
          var validateQ: Double = 5.0
          var reusablew: String! = String(cString: [106,100,104,117,102,102,0], encoding: .utf8)!
          var requestu: String! = String(cString: [117,110,114,101,99,111,103,110,105,122,101,100,0], encoding: .utf8)!
          var calls: String! = String(cString: [97,97,99,115,98,114,100,97,116,97,0], encoding: .utf8)!
         fileH |= 2 + requestu.count
         validateQ /= Swift.max((Double((String(cString:[68,0], encoding: .utf8)!) == reusablew ? reusablew.count : Int(validateQ))), 3)
         requestu.append("\(reusablew.count)")
         calls.append("\(calls.count)")
         if fileH == 3458602 {
            break
         }
      } while (fileH == 3458602) && ((fileH >> (Swift.min(labs(5), 1))) == 2)
      repeat {
         versionK = "\(versionK.count - Int(table_))"
         if 1607380 == versionK.count {
            break
         }
      } while (Int(table_) == versionK.count) && (1607380 == versionK.count)
       var menuL: String! = String(cString: [99,111,101,102,102,115,0], encoding: .utf8)!
       var versionlabelq: String! = String(cString: [101,110,99,111,100,101,100,102,114,97,109,101,0], encoding: .utf8)!
      displayi /= Swift.max(Double(3 % (Swift.max(4, fileH))), 5)
   }

       var modityu: Float = 5.0
       var navgationv: String! = String(cString: [104,101,118,99,0], encoding: .utf8)!
      repeat {
         navgationv.append("\(navgationv.count % 3)")
         if (String(cString:[57,115,53,114,48,98,95,122,104,97,0], encoding: .utf8)!) == navgationv {
            break
         }
      } while (1.39 > modityu) && ((String(cString:[57,115,53,114,48,98,95,122,104,97,0], encoding: .utf8)!) == navgationv)
         modityu += Float(navgationv.count << (Swift.min(5, labs(Int(modityu)))))
         modityu -= Float(navgationv.count)
      repeat {
          var flow8: String! = String(cString: [115,108,117,103,0], encoding: .utf8)!
          var int_9Q: Double = 1.0
         modityu /= Swift.max(Float(flow8.count), 2)
         flow8 = "\(3 | Int(int_9Q))"
         int_9Q -= Double(Int(int_9Q))
         if modityu == 1198505.0 {
            break
         }
      } while (navgationv.count < Int(modityu)) && (modityu == 1198505.0)
      if modityu >= 4.35 {
          var graphicsq: Double = 3.0
          var delegate_jyF: String! = String(cString: [115,101,108,102,0], encoding: .utf8)!
          var browserq: Int = 2
          var iosO: Double = 2.0
         modityu *= Float(Int(iosO))
         graphicsq *= Double(Int(graphicsq))
         delegate_jyF.append("\(delegate_jyF.count ^ Int(graphicsq))")
         browserq -= delegate_jyF.count * Int(graphicsq)
         iosO -= Double(3 ^ Int(graphicsq))
      }
         navgationv.append("\(Int(modityu))")
      displayi *= Double(3 + navgationv.count)
        super.setSelected(selected, animated: animated)

    }

@discardableResult
 func netVolumeRightBoxImageView(progressElevt: String!) -> UIImageView! {
    var indexd: Float = 5.0
    var window_lkM: [String: Any]! = [String(cString: [120,112,117,98,0], encoding: .utf8)!:613, String(cString: [97,118,99,116,120,0], encoding: .utf8)!:285, String(cString: [99,113,117,101,117,101,0], encoding: .utf8)!:882]
   repeat {
       var sectionL: Double = 5.0
       var detailslabelb: Double = 1.0
       var freee: String! = String(cString: [100,101,113,117,97,110,116,105,122,101,0], encoding: .utf8)!
       var convertedl: Int = 4
       var firstB: Bool = false
         freee.append("\(1)")
      while ((sectionL - detailslabelb) >= 5.22) {
         detailslabelb -= Double(Int(detailslabelb) - Int(sectionL))
         break
      }
          var recordsV: String! = String(cString: [118,105,122,105,101,114,0], encoding: .utf8)!
          var showP: Int = 3
          var writeO: Double = 4.0
         firstB = recordsV.count >= Int(detailslabelb)
         recordsV.append("\(showP / 2)")
         showP -= Int(writeO) / 2
         writeO /= Swift.max(1, Double(3))
      while ((detailslabelb / 3.58) == 5.80 || 3.58 == (Double(convertedl) / (Swift.max(detailslabelb, 3)))) {
         convertedl %= Swift.max(3, ((firstB ? 1 : 3) / (Swift.max(Int(detailslabelb), 5))))
         break
      }
      if 5.85 <= sectionL {
         firstB = (String(cString:[53,0], encoding: .utf8)!) == freee
      }
      for _ in 0 ..< 1 {
          var recordsT: String! = String(cString: [115,117,99,99,101,115,115,0], encoding: .utf8)!
         convertedl -= Int(detailslabelb) << (Swift.min(1, labs(3)))
         recordsT.append("\(recordsT.count)")
      }
          var delegate_l5: String! = String(cString: [105,110,111,117,116,0], encoding: .utf8)!
         firstB = 21 < (Int(Int(detailslabelb) / (Swift.max(9, convertedl))))
         delegate_l5 = "\(delegate_l5.count << (Swift.min(delegate_l5.count, 1)))"
      repeat {
         detailslabelb *= Double(Int(detailslabelb) & 1)
         if detailslabelb == 3890074.0 {
            break
         }
      } while (detailslabelb == 3890074.0) && (detailslabelb > 3.15)
      while (!firstB) {
         firstB = (String(cString:[97,0], encoding: .utf8)!) == freee
         break
      }
         firstB = freee == (String(cString:[78,0], encoding: .utf8)!) && 77.80 == sectionL
         sectionL *= Double(Int(detailslabelb))
      while (detailslabelb >= 5.48) {
          var nextv: Float = 4.0
          var graphicsj: Double = 4.0
          var picturea: Bool = true
          var messagesg: Float = 5.0
          var unselectedl: String! = String(cString: [107,101,121,115,112,101,99,95,49,95,52,53,0], encoding: .utf8)!
         detailslabelb *= (Double(Int(nextv) >> (Swift.min(5, labs((firstB ? 1 : 2))))))
         nextv -= Float(3)
         graphicsj -= Double(3)
         picturea = messagesg == 90.61
         messagesg /= Swift.max(Float(2 << (Swift.min(labs(Int(messagesg)), 4))), 1)
         unselectedl.append("\(3)")
         break
      }
         firstB = 62 == convertedl
       var t_heighti: String! = String(cString: [99,111,115,113,102,0], encoding: .utf8)!
      if (5.35 / (Swift.max(5, sectionL))) == 3.64 || (sectionL - 5.35) == 4.13 {
          var chath: String! = String(cString: [111,98,115,101,114,118,105,110,103,0], encoding: .utf8)!
          var avatarm: [Any]! = [535, 824, 421]
         sectionL -= Double(3)
         chath.append("\(avatarm.count & 1)")
         avatarm = [chath.count >> (Swift.min(labs(1), 3))]
      }
      window_lkM = ["\(window_lkM.values.count)": 3 + window_lkM.count]
      if window_lkM.count == 2316917 {
         break
      }
   } while (window_lkM.count == 2316917) && (window_lkM.values.count < 2)
   if (4.35 + indexd) > 2.43 {
      indexd *= Float(Int(indexd))
   }
   for _ in 0 ..< 2 {
      indexd -= Float(Int(indexd))
   }
      window_lkM["\(indexd)"] = 3 ^ Int(indexd)
     let yhlogoBottom: [String: Any]! = [String(cString: [112,105,120,101,108,115,120,0], encoding: .utf8)!:String(cString: [115,104,111,117,108,100,0], encoding: .utf8)!, String(cString: [98,97,114,0], encoding: .utf8)!:String(cString: [116,105,108,108,0], encoding: .utf8)!]
     let reusableVip: UIImageView! = UIImageView(image:UIImage(named:String(cString: [98,111,114,105,110,103,115,115,108,0], encoding: .utf8)!))
     var cellFlow: Double = 3284.0
    var docUncoded = UIImageView(frame:CGRect(x: 284, y: 187, width: 0, height: 0))
    reusableVip.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    reusableVip.alpha = 1.0
    reusableVip.frame = CGRect(x: 158, y: 114, width: 0, height: 0)
    reusableVip.contentMode = .scaleAspectFit
    reusableVip.animationRepeatCount = 8
    reusableVip.image = UIImage(named:String(cString: [100,105,115,109,105,115,115,0], encoding: .utf8)!)
    
    var reusableVipFrame = reusableVip.frame
    reusableVipFrame.size = CGSize(width: 140, height: 279)
    reusableVip.frame = reusableVipFrame
    if reusableVip.alpha > 0.0 {
         reusableVip.alpha = 0.0
    }
    if reusableVip.isHidden {
         reusableVip.isHidden = false
    }
    if !reusableVip.isUserInteractionEnabled {
         reusableVip.isUserInteractionEnabled = true
    }

    docUncoded.alpha = 1.0;
    docUncoded.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    docUncoded.frame = CGRect(x: 153, y: 233, width: 0, height: 0)
    docUncoded.image = UIImage(named:String(cString: [114,101,99,111,114,100,105,110,103,118,0], encoding: .utf8)!)
    docUncoded.contentMode = .scaleAspectFit
    docUncoded.animationRepeatCount = 9

    
    var docUncodedFrame = docUncoded.frame
    docUncodedFrame.size = CGSize(width: 261, height: 137)
    docUncoded.frame = docUncodedFrame
    if docUncoded.alpha > 0.0 {
         docUncoded.alpha = 0.0
    }
    if docUncoded.isHidden {
         docUncoded.isHidden = false
    }
    if !docUncoded.isUserInteractionEnabled {
         docUncoded.isUserInteractionEnabled = true
    }

    return docUncoded

}





    
    override func awakeFromNib() {

         let assertIlbc: UIImageView! = netVolumeRightBoxImageView(progressElevt:String(cString: [115,117,98,115,99,114,105,98,97,98,108,101,0], encoding: .utf8)!)

      if assertIlbc != nil {
          let assertIlbc_tag = assertIlbc.tag
          self.addSubview(assertIlbc)
      }



       var picu: Double = 1.0
    var area8: Int = 5
   for _ in 0 ..< 1 {
      area8 >>= Swift.min(labs(3), 3)
   }
      picu += Double(Int(picu) / (Swift.max(8, area8)))

      picu /= Swift.max(Double(Int(picu) >> (Swift.min(3, labs(area8)))), 2)
       var gundu: Bool = true
       var alifastx: Bool = true
      repeat {
          var q_animation4: String! = String(cString: [116,105,109,101,115,99,97,108,101,0], encoding: .utf8)!
         gundu = gundu || !alifastx
         q_animation4.append("\(((String(cString:[85,0], encoding: .utf8)!) == q_animation4 ? q_animation4.count : q_animation4.count))")
         if gundu ? !gundu : gundu {
            break
         }
      } while (gundu ? !gundu : gundu) && (alifastx && gundu)
       var level_: String! = String(cString: [115,116,114,101,97,109,102,111,117,114,99,99,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
         level_.append("\(1)")
      }
      repeat {
         alifastx = !level_.hasPrefix("\(alifastx)")
         if alifastx ? !alifastx : alifastx {
            break
         }
      } while (alifastx ? !alifastx : alifastx) && (!alifastx)
      repeat {
          var fileq: String! = String(cString: [108,111,99,107,109,103,114,0], encoding: .utf8)!
          var obj2: Double = 3.0
          var vipiconp: Int = 5
          var weixinlabelY: Bool = true
         level_ = "\(((alifastx ? 3 : 5)))"
         fileq = "\(Int(obj2))"
         obj2 += Double(fileq.count)
         vipiconp <<= Swift.min(labs(vipiconp), 1)
         weixinlabelY = weixinlabelY || vipiconp == 1
         if level_.count == 396083 {
            break
         }
      } while (!gundu) && (level_.count == 396083)
      repeat {
          var weak_iwI: String! = String(cString: [97,97,99,101,110,99,116,97,98,0], encoding: .utf8)!
          var rowr: String! = String(cString: [105,100,120,0], encoding: .utf8)!
         level_ = "\(weak_iwI.count - 3)"
         weak_iwI = "\(3)"
         rowr.append("\((rowr == (String(cString:[69,0], encoding: .utf8)!) ? rowr.count : rowr.count))")
         if (String(cString:[102,111,109,107,114,51,0], encoding: .utf8)!) == level_ {
            break
         }
      } while (gundu) && ((String(cString:[102,111,109,107,114,51,0], encoding: .utf8)!) == level_)
      picu -= (Double((alifastx ? 5 : 3) / (Swift.max(1, area8))))
        super.awakeFromNib()
        
        
    }
    
    
}
